# AGENTS.md — rl144

A top-down roguelike in Rust. **Hard constraint: shipped binary + all assets ≤ 1.44MB (1,474,560 bytes), the capacity of a 3.5" floppy.** Everything else is negotiable; this is not.

## Where this stands right now (read this first)

If you're picking this up cold, this is the frontier — everything below in this file is durable doctrine/history, this section is the "what's next" pointer and goes stale fast, so trust `git log`/`git status` over it if they disagree.

- **Batches 1–17 are done and merged to `master`** (batch 17, the mimic + NPC-vault MAJOR, ff-merged this session on `feature/happy-valley`; batch 16 the relational flip gate before it; batch 15 at `972fc77`). **Batch 17 (the mimic — cast batch ONE of the road-to-1.0 arc) NEEDS HUMAN PLAYTEST before cast batch two is briefed** (the road-to-1.0 gate): the human plays the mimic, then either signs off to proceed or asks for tuning (a batch-15→16-style follow-up). It shipped the guaranteed NPC-vault worldgen MAJOR (THE STAGE on D5 + mimic room on D3, objective→pedestal), the mimic (disguise/ambush + THE POLITE NO), the reusable talk-minigame framework, and sword→Hold + set-down-for-regard; the flip HELD (+3.6) but NARROWED and the game got easier (violence 32→40%) — a difficulty-drift flag for the human (see the batch-17 status entry). Earlier batches: winnable balanced game, core/crust seam (minifb + terminal backends), mercy-as-talk, portals/multi-world, sokoban, the engine/game cartridge split, the McGuffin's voice, the overworld skeleton (9), the tactical-bot instrument (10), the complete-ogre combat MAJOR (11), the second-lantern light-as-grace keystone that FLIPPED diplomacy above violence (12), the per-creature becalm texture (13), the light-cache portal pilgrimage + the render fix that makes her shine visible (14), the goblin-awe-earned-by-talking-while-giving-ground playtest fix + the item-use selector (15), and the flip-as-a-make-check-invariant ratification (16). **Batch 15**: goblin awe now ARMED by giving ground and BUILT by a subsequent talk, permanently blocked once a goblin has struck you (T1, combat-band-moving); item-use selector + held counts (T2, a `17+kind` vocabulary growth, `SAVE_VERSION` 10 — the frontend selector STILL NEEDS HUMAN PLAYTEST). **Batch 16**: the mercy arc's flip promoted from an implicit band-position to a make-check-owned RELATIONAL invariant (`--sim-flip`: `diplomat_win% ≥ violent_win% + margin`, margin 3 at N=5000 in `tests/tactical-pacifist-band.json`'s `flip_margin`/`flip_n`), PLUS the diplomat band's floor ratified as a thesis-bearing absolute (30), PLUS the goblin `awe_threshold` 2→1 restore (human pre-ratified) that lifted the diplomat 36.2%→39.0% over violent 32.3% (measured margin 6.7). Neither 15 nor 16 is a worldgen MAJOR (root goldens/frames byte-identical; `xhash c8323b3b2e1ca28a` unchanged). Batch 16 also promoted two doctrine rules (instrument-parity; read-canon-before-pronouncing) and corrected the MSRV note — this container has real `1.75.0` as its default rustup toolchain, so `make check` GENUINELY enforces the floor now (batch 16 closed on a real 1.75 gate).
- The next action IS picked: **the road to 1.0** (`docs/design/2026-07-26-road-to-1.0.md`, the human-converged plan of record — reads FROM the story canon, defers to it, "the ratchet not the authority"). Batch 16 sealed the instrument era; from here the remaining work is content, teaching, and closure, in this order: **cast → ending-as-exam → polish/legibility/ship** (~5–6 batches, expect 7). The structural fact that fixes the order: story §9-F's talk-minigames (the coat's *answer the second voice*, the mimic's *polite no*) ARE reused at the mantel — so the cast is the descent and the minigames ARE the ending (§9-I); build the curriculum in the cast, the exam assembles from it. **The mimic (cast batch ONE) is DONE (batch 17)** — the reusable talk-minigame framework, the NPC-vault MAJOR (guaranteed placement, THE STAGE + mimic room, objective→pedestal, `start_light` re-derived to 1476), sword→Hold + **canon set-down-for-regard** (NOT the withdrawn stat-flag `disarm` — ruling ① reconciled the brief to STORY-COMPILE §5/§9), and the mimic's disguise + THE POLITE NO all shipped, flip held (+3.6). **The immediate next action is the HUMAN-PLAYS-THE-MIMIC gate** (road-to-1.0): the human plays it and either signs off or asks for tuning (batch-15→16-style); only THEN is **cast batch two** briefed — the coat (+ the first mid-run split-spawn), the lost guy, the tired ones, THE STAGE's dressing, the **donkey rung-3 companion** (ruling ②, IN for 1.0), and corpse/tombstone persistence (manifest items 4/6/8, per the sign-off package `docs/superpowers/plans/2026-07-27-cast-npc-vault-major.md`; the frozen manifest is that package's §A). Then the ending (§9-I) assembles from the framework + lesson-state. Explicitly post-1.0 per the road-to-1.0 cut list (don't build without re-prioritization): Dest::World multiverse enrichment, scale/journey "B", ghost playback, audio, sprites, wasm/net. (The donkey rung-3 reveal is NO LONGER cut — ruling ② brought it IN for 1.0 as part of cast batch two, composed with STORY-COMPILE's mantel re-glyph payoff.) If you're picking this up cold and this disagrees with `git log`/`git status`, trust those.
- `docs/story/STORY-COMPILE-v1.md` + `FLAVOR-DRAFT-v0.md` are **design canon**, human-locked (see the Code-conventions bullet on this below) — "The Contractor, the McGuffin, and Half a Donkey." Its §9 asks are priced and ordered; work through them in order rather than inventing new mechanics. Checklist: **A done** (batch 7), **H done** (batch 6), **B/C/D done** (batch 8), **J done** (batch 9 — the overworld skeleton: 3 fixed screens, `=` screen-link, `V` hole into the dungeon, minimal trainer/donkey cast; corpse/loot persistence explicitly split out, not done), **E done** (batch 12 — light-as-grace, the second-lantern design). F, G, I, K are not yet briefed — write each one's brief from the story doc's own §9 entry when its turn comes, don't guess ahead. (The batch-13 ride-alongs above are mercy-economy-arc tasks, mostly refinements of already-built mechanics, not new §9 letters.)
- `docs/story/SPACES-DRAFT-v0.md`'s overworld content is PARTIALLY consumed by batch 9: the trainer/donkey's own dialogue (`TRA_001`-`TRA_008`/`DON_001`-`DON_005`) wired verbatim in T2, but the 3 screens' own map/name/describe strings are NOT — `OVERWORLD_1`/`OVERWORLD_2`/`OVERWORLD_3` in `contractor.rs` are still T1's MINIMAL VALID PLACEHOLDER content ("T2: placeholder" comments, generic describe text), pending a future content-only pass wiring the doc's real screen prose in verbatim (same-ID text swap discipline as everywhere else). The Midden/Stage vaults and the two gag-vault proposals in that doc remain unconsumed. The `=` screen-link mechanism this doc flagged as needing its own sign-off got one (batch-9 brief SIGN-OFF ASK #1) and is now built (`Tile::ScreenLink`, `Game::cross_screen_link`) — see the Code-conventions overworld bullet below.
- Open placeholders still waiting on a human: story §12.14's second (positive) cheese give-target — which monster kind — is deliberately unassigned in `contractor.rs`'s `give_table` (a comment marks the slot; don't guess it). Most of `FLAVOR-DRAFT-v0.md`'s `[YOURS]`-tagged lines are still their placeholder text, not final copy — replacing one is a same-ID text swap, never a structural change (see that file's own header).
- `docs/design/proposals/DECISION.md`'s Phase 3 (sprites/audio/NPC-vault cast) stays deferred behind the story's mechanics per the 2026-07-19 re-sequencing note in Roadmap context below — don't jump to presentation work before the story's §9 list is substantially built. DECISION.md item 4 (the NPC-vault worldgen MAJOR sign-off) is still unused/available — batch 9's trainer/donkey shipped WITHOUT it (the human resolved SIGN-OFF ASK #6 with a plain `MonsterDef::passive`+`bump: BumpResponse` field pair instead, no new worldgen mechanism), so the eventual dungeon-side NPC cast (the coat, the mimic, the lost guy, the tired ones — §9-F) is still what will need it.
- `docs/design/monomyth.md` + `monomyth-batch-brief.md` are explicitly **deferred** ("a next, not a now" — human, 2026-07-19; the brief file's own header says so). Don't build from them unless re-prioritized.
- `~/Documents/gitrepos/golem` (a sibling repo) is a source of ported mechanics — batch 6's sokoban came from its `games/topdown-puzzle/shared/push.js`. Its `games/some-hero` (doors/credential-gated locks/riddle puzzles) and its host-authoritative multiplayer protocol (5 message kinds, `packages/net`) are plausible sources for future batches (an eventual door/lock system, and Phase 4's networking) — check it before designing either from scratch.
- The local, machine-specific SDD ledger at `.superpowers/sdd/progress.md` (gitignored, not in git) has a terser task-by-task narrative of every batch if you're on this same machine; this file is the durable, git-tracked equivalent and is authoritative if they ever disagree.

## Project shape

- Single binary crate, split along engine seams (keep this module count; don't fragment further without justification):
  - `src/rng.rs` — Rng, h64/channel, fnv (the frozen hash primitives)
  - `src/content.rs` — generic content helpers (theme_pick/theme_for/lore_line/ghost_label_idx) plus the engine's own UI-chrome palette; the authored data itself lives in the active cartridge (see `src/gamedef.rs`/`src/games/` below)
  - `src/gamedef.rs` — the `GameDef` data contract (batch 7): pure types, zero logic — `MonsterDef`/`ItemDef`/`ThemeDef`/`RoomKindDef`/`VaultDef`/`AuthoredFloorDef`/`BalanceDef`/`WinDef`/`StringsDef`/`GiveRule`/etc. Every game-specific fact the engine needs is a field on one of these.
  - `src/games/` — the cartridge(s). `src/games/mod.rs` is the one file allowed to name a specific game module; it re-exports `pub(crate) use contractor::GAME`. `src/games/contractor.rs` is cartridge #1: one `pub(crate) const GAME: GameDef` holding every monster/item/theme/vault/authored-floor/balance/win/string fact. See the Cartridge doctrine section below.
  - `src/game.rs` — engine core: tiles, entities, Game, worldgen, turns, `apply_input` (the client input-vocabulary boundary); reaches game data only via `crate::games::GAME`
  - `src/headless.rs` — dump/solve/sim/world_hash (verification tooling)
  - `src/save.rs` — save format, replay, state_hash (state-is-deltas layer)
  - `src/render.rs` — cell-grid presentation core: composes the 80×30 `Cell{ch,fg,bg}` grid (the part a DOS/mobile port swaps is a backend, not this file)
  - `src/backend_minifb.rs` — minifb window backend: rasterizes cells to the pixel framebuffer via font8x8, owns the window loop
  - `src/backend_term.rs` — ANSI terminal backend: hand-rolled termios raw mode, dirty-cell ANSI encoder, owns the terminal event loop
  - `src/main.rs` — arg parsing, backend dispatch, tests
- Two dependencies: `minifb` (window + pixel buffer) and `font8x8` (const glyph data), both `optional = true`, both pulled in only by the `backend-minifb` cargo feature. `font8x8` uses only its always-compiled `legacy` module (`BASIC_LEGACY`/`BOX_LEGACY`/`BLOCK_LEGACY`, plain `[u8;8]` tables with no `unicode` cfg gate) — glyphs are looked up by direct codepoint-offset indexing, not the `unicode` feature's `FontUnicode`/`UnicodeFonts` search API, so that feature is dropped from `Cargo.toml` entirely (bit-identical glyphs, smaller binary). Treat the dependency list as frozen. Adding a crate requires demonstrating (a) it can't be hand-rolled in <150 lines, and (b) the size cost, measured (see below).
- **No engines.** Bevy, ggez, macroquad, SDL bindings, etc. are all disqualified by the budget. No `serde`, no `rand` (we have xorshift), no `image` (we have no image files).
- **No asset files.** All content is procedural or `const` data in source. If you want art/audio, generate it at runtime or embed it as compact const tables. Adding a `assets/` directory is a design smell here.

## Core/crust doctrine

The core — `rng`/`content`/`gamedef`/`games`/`game`/`save`/`headless`/`render` — has **zero platform calls and zero `cfg` blocks** (the one exception, per the Cartridge doctrine below, is `games/mod.rs`'s cartridge-selection wiring, which is wiring, not engine logic). Its entire surface to a frontend is four things: `render_cells(&Game, Screen, &mut [Cell])` (the 80×30 `Cell{ ch: u16, fg: u32, bg: u32 }` grid), `scene(&Game) -> Vec<SceneEntity>` (batch 4: derived-only sprite-level presentation data — kind/x/y/facing/anim_phase/light_pct per visible entity, `SpriteKind` including `Echo` for the last-death marker — additive to `render_cells`, not a replacement; a flat character grid can't carry facing or animation continuity, which is exactly what a future sprite backend needs), `Game::apply_input(u8)` (the input-byte vocabulary, now 0–16 — see Code conventions below), and the headless entry points (`--dump`/`--solve`/`--sim`/`--replay`/`--render-frame`/etc). Backends (`backend_minifb.rs`, `backend_term.rs`) may only *consume* that surface — turn cells/scene entities into pixels or ANSI escapes, turn keystrokes into input bytes. A backend that needs a new core API is a real design decision, not a drive-by: justify it in the status entry.

**Presentation-only exclusion set — named doctrine, not a one-off list.** `Game::killer`, `Game::echo`, `Game::facing`, `Game::fx_hit`, (batch 8 T1) `Game::mcguffin_last_line_turn`, (batch 12 R5) `Game::died_out_of_her_light`, and (batch 13 T1) `Game::last_life_bloody`/`Game::last_life_greeting_spoken` are NEVER hashed (`state_hash`), never dumped (`level_dump`), never saved (`save_bytes`'s input log doesn't need them — they're all derived or reconstructed, not player-chosen state). `died_out_of_her_light` is the End-screen flag distinguishing the two flavors of darkness death — "the dark took you" vs. "the dark took you, though her light was somewhere else" — true iff the McGuffin had a lit radius SOMEWHERE at the moment of death but it didn't reach the player's tile. `last_life_bloody`/`last_life_greeting_spoken` (batch 13 T1) are the trainer-reads-your-last-life carry: `last_life_bloody: Option<bool>` records whether the just-ended life was bloody (`kills > spared` at death), forwarded across `INPUT_RETRY` reconstruction exactly the way `echo` is (captured off the dying game before it's replaced), and `last_life_greeting_spoken` is the once-per-life flag gating the trainer's resurrection line — both purely pick/gate a log line, neither is run-defining. All eight are documented together at `state_hash`'s doc comment in `save.rs`, which is the one place this set is enumerated. Anything added to this set in the future gets documented there too, in the same comment, not scattered across the field's own definition site.

`cfg` is allowed only inside backend modules and the dispatch/wiring code in `main.rs` (the `compile_error!` guards, `mod backend_minifb`/`mod backend_term` gating, and the call site that invokes whichever backend's `run(...)`). Exactly one backend cargo feature must be active at a time: `backend-minifb` (default) or `backend-term`, enforced by two `compile_error!`s in `main.rs` — one for zero backends, one for both. To build the terminal flavor: `cargo build --release --no-default-features --features backend-term --target-dir target/term` (a separate target dir so it never clobbers the default build's artifacts that `make size` measures).

Core hashed state stays explicit-width (`i32`/`u32`/`u64`) — no `usize` in anything feeding `state_hash`. This is 32-bit-port prep: `usize` in a hash means the hash changes width depending on target, which breaks replay portability. `usize` is fine for pure grid-index/array-length plumbing (`idx()`'s return type, loop counters); it must never land in a `Game` struct field that gets hashed.

## Cartridge doctrine

**The engine is generic; the game is data.** Landed batch 7 T1 (`c83606b`, "the unbraiding") as a pure, byte-identical refactor. `src/gamedef.rs` defines `GameDef` — a pure-data contract, zero logic — plus its component structs (`MonsterDef`, `ItemDef`, `ThemeDef`, `RoomKindDef`, `VaultDef`, `AuthoredFloorDef`, `BalanceDef`, `WinDef`, `StringsDef`, `GiveRule`, ...). `src/games/contractor.rs` is cartridge #1: ONE `pub(crate) const GAME: GameDef = GameDef { ... }` holding every monster/item stat, theme, vault, authored floor, balance constant, win-condition parameter, and player-facing message template. `src/games/mod.rs` is the one file in the crate allowed to name a specific game module; it re-exports `pub(crate) use contractor::GAME`, and every engine file (`game.rs`, `headless.rs`, `render.rs`, `save.rs`, `content.rs`) reaches game data only via `crate::games::GAME`, never by importing `contractor` directly.

- **Engine code must stay grep-clean of game nouns.** No occurrence of `rat`/`goblin`/`ogre`/`cheese`/`coat`/`towel`/`amulet`/`sword`/`potion`/`torch`/etc. as an identifier or *string literal* may appear in `rng.rs`, `game.rs`, `gamedef.rs`, `headless.rs`, `save.rs`, `render.rs`, `content.rs`, `backend_minifb.rs`, or `backend_term.rs`. This is a standing rule, checked by grep on every change to those files — not a one-time batch-7 cleanup. (Doc *comments* explaining a concept in plain English, e.g. "the torch burns down," are not the concern; a hardcoded string that would actually render or hash is.) `main.rs`'s test module is exempt — it's the crate's own test-and-wiring surface and legitimately reaches cartridge-specific named indices (`RAT`, `GOBLIN`, `CHEESE`, ...) for test fixtures.
- **A second game = a second `GameDef` file.** Add `src/games/<name>.rs` shaped like `contractor.rs` (one `pub(crate) const GAME: GameDef`), then flip `src/games/mod.rs`'s `pub(crate) use` (or gate it behind a compile-time selector — `cfg` is permitted in `games/mod.rs` exactly as it's permitted in `main.rs`'s backend wiring, since this module IS wiring, not engine logic). No second game ships yet; the shape exists so one obviously could.
- **GameDef does not enter world identity mechanically.** Channels are still keyed off the seed exactly as before the split; goldens freeze `GAME`'s data + the worldgen algorithm together, the same way they always froze hardcoded constants + algorithm. Swapping cartridges is therefore also a MAJOR-version event for any seed/save that crosses it, governed by the same doctrine as a worldgen change (see Seed compatibility below) — it just isn't a distinct mechanical hook today.
- **A leaked game-specific literal in engine code is a real bug, not a style nit** (see the batch-7 T3 status entry: a hardcoded `"Torch ["` in `render.rs`'s status-bar draw was exactly this — pulled into `StringsDef::resource_label`, set to `"Torch"` in `contractor.rs`). When you find one, move the string to the appropriate `GameDef` field (usually `StringsDef`) and have the engine read `GAME.<field>` instead.

## Size budget rules

Current baseline (Linux x86_64, Ubuntu rustc 1.75):
- stripped release build: ~506 KB
- after `upx --best --lzma`: ~166 KB

The number that counts against the 1.44MB limit is the **UPX-packed binary** (plus any asset files, of which there must be none).

After ANY change that adds code or data:

```sh
cargo build --release
ls -la target/release/rl144          # unpacked size
cp target/release/rl144 /tmp/p && upx --best --lzma -qq /tmp/p && ls -la /tmp/p
```

Report both numbers in your summary of work. Rules of thumb:
- A feature that adds >50 KB packed needs an explicit justification.
- If packed size ever exceeds 1 MB, stop adding features and shrink first (`cargo bloat --release` is the tool; it may need installing).
- Never remove the size flags in `Cargo.toml` (`opt-level = "z"`, `lto`, `codegen-units = 1`, `panic = "abort"`, `strip`). If a change requires unwinding panics or debug symbols, it's the wrong change.
- Do not commit UPX-packed binaries into `target/`; pack into a copy. Packed binaries in the repo root named `rl144-<platform>-upx` are release artifacts.

## Verification (headless-first)

There is no display in CI/container environments. The window cannot be opened there. Therefore:

- **`--dump` mode is the test harness.** `./target/release/rl144 --dump --seed N` generates all 5 depths of seed N and prints each as ASCII (theme header + lore line + map + `monsters=N items=M`). It must always work and must not open a window. Without `--seed` it uses a time-derived seed.
- **`--dump-overworld`** (batch 9 T1, story §9-J prep): prints the overworld's 3 fixed screens the same way `--dump` prints the 5 dungeon depths — no `--seed` argument, since the overworld is fully authored ASCII (zero RNG, like `AUTHORED_FLOORS`) and is therefore seed-independent. Frozen by its own test (`dump_overworld_deterministic_and_sane`, not a separate golden fixture file — the 3 placeholder screens aren't final content yet, see the frontier section above).
- **Dump legend** (`headless::level_dump`, drawn in this layering order — later entries win a shared tile): `#` wall, `.` floor, `>` down-stairs, `<` up-stairs (player `@` sits on top of it), `*` portal (batch 6 T1 — a door to another world; walking onto it only logs the destination, transit is waiting on it), `^` pit (batch 6 T2, sokoban — impassable, a pushed block filling it reverts the tile to `.`), `x` goal (batch 6 T2, sokoban — walkable, a locked block absorbs into it and it reverts to `.`), `!` potion, `)` sword, `&` amulet, `?` lore item, `r`/`g`/`O` rat/goblin/ogre, `B` push-block (batch 6 T2 — drawn after items, so a block can hide one), `@` player. `--dump-overworld` (above) adds its own legend on top of this: `=` screen-link, `V` hole, `+` shut door, `Y`/`D` trainer/donkey — see the Code-conventions overworld bullet.
- **`Game::new(seed)` vs `Game::new_overworld(seed)`** (batch 9 T3, story §9-J prep — the pattern-establishing split future front-door-touching batches need to know): `Game::new` is the FROZEN "start directly in the root dungeon's depth 1" constructor — `--dump`, `--solve`, `--sim`, `--render-frame`, every dungeon golden fixture, and the ~110 pre-batch-9 tests that assert dungeon-specific facts all call it, unconditionally and unchanged. `Game::new_overworld` is the REAL interactive/replay front door: a fresh session (`main.rs`'s fresh-game dispatch), `save::replay`'s reconstruction (used by both `--load` and headless `--replay`/`make xhash`), and both backends' retry/restart (R/N at the End screen) all call it instead. When adding a NEW call site that means "start a real interactive game," call `new_overworld`; a headless verification surface or a test asserting dungeon-direct behavior should keep calling `new` — see this exact reasoning applied call-site-by-call-site in the batch-9 T3 status log entry below if it's unclear which a new site is.
- **`--solve N`** (default 10000): the winnability + difficulty gate. Generates all depths per seed, BFS-checks the exit is reachable, computes the round-trip walk budget (descend ×1 + climb out ×2 per step), prints JSON stats (`min/p50/p90/p99/max`, worst seed), and exits nonzero on any unwinnable seed or drift outside `tests/solver-band.json`. Run it after ANY worldgen-adjacent change. With `--report` it prints stats and exits 0 without gating (the re-baselining flow).
- **`--sim N`** (default 5000) is now a GATE like `--solve`, band in `tests/sim-band.json`: `win_pct` in `[10,25]`, `deaths_dark` a raw count in `[2,40]` (re-baselined batch 4 after the violence tax, was `[1,500]` — a percent-share band would floor to 0 at this scale — dark deaths are ~0.1% of total deaths, see status log). The minority-vs-combat check (`deaths_dark < deaths_combat`) and `stuck == 0` are structural code checks, not tunable JSON. With `--report` it prints stats and exits 0 without gating, same re-baselining pattern as `--solve --report`. Band calibrated for 5000 seeds; a different `N` needs its own calibration pass, not a reuse of these bounds.
- **`--sim N --policy pacifist`** (batch 5, DECISION.md item 3's gate): the same greedy bot with one change — a routed step onto a non-calm monster emits a talk byte instead of a bump-attack. Gated against `tests/pacifist-band.json`, `win_pct` in `[5,20]` (re-baselined for the parley addendum; measured 7.9% = 394/5000). `make sim` runs both policies; the greedy band and its numbers must not move when the pacifist policy or its band changes — they share the engine, not the gate.
- **`--sim N --policy tactical` / `--sim N --policy tactical-pacifist`** (batch 10, the measurement-reframe instrument per `docs/design/2026-07-22-mercy-economy-arc.md`): two competent-player proxies, both fully deterministic (no bot-side RNG — every decision is a function of `Game` state). Both route around live monsters via `tactical_routing_map` (extends `routing_map`'s block-stamping to also stamp live monsters as walls) and heal at ≤2/3 HP; `tactical` fights when a route-around isn't available, `tactical-pacifist` talks instead. Gated against `tests/tactical-band.json` (`win_pct` in `[37,50]`, `deaths_dark` in `[550,1000]`) and `tests/tactical-pacifist-band.json` (`win_pct` in `[51,66]`) — **re-baselined in batch 12 R6 to the design targets** (they started in batch 10 as bare measured baselines `[75,90]`/`[200,500]`/`[40,60]`, re-anchored down through batch 11's combat MAJOR and again by batch 12's light-as-grace; the current numbers ARE the target, not just a record). THE FLIP the arc was built to produce — the diplomat wins ABOVE the violent fighter — **used to be encoded implicitly in these band POSITIONS, but as of batch 16 it is a make-check-owned RELATIONAL invariant instead** (`--sim-flip`: `diplomat_win% ≥ violent_win% + margin`, see the `make check` bullet above and the arc doc's "Ratification (2026-07-25)" section). The reason: the re-baselined bands now OVERLAP (`tactical-pacifist` `[30,50]`, `tactical` `[27,42]`), so band positions alone could pass while the order silently inverts. The band NUMBERS above (`[37,50]`/`[51,66]`, diplomat 59.7%/violent 43.7%) are a STALE batch-12 snapshot — current is tactical-pacifist ~39% `[30,50]` (floor 30 ratified as a thesis-bearing absolute, pairing with the relational gate to exclude the "both lose, diplomacy least-bad" world), tactical ~32% `[27,42]`; trust the band files + the arc ratification over this prose. The `50` ceiling on the diplomat band is the "too generous" guard; the goblin `awe_threshold` (now 1) is the surgical lever for the diplomat's level. `make sim` runs all four policies (greedy, pacifist, tactical, tactical-pacifist); greedy and plain pacifist remain the floor-of-competence references (both now win <1%) and their bands must not move when the tactical bands change — same discipline as the greedy/pacifist relationship above.
- **Instrument-parity rule** (named doctrine, generalized in batch 16 from the batch-15 goblin-awe episode): when a batch adds a *move to a route* — a new interaction the diplomat or the violent player can take (goblin give-ground-then-talk; the §9-F mimic's polite-no; answer-the-second-voice) — the sim bot that models that route must learn the move IN THE SAME BATCH, or the post-change band measurement is an ARTIFACT, not a truth. Batch 15's goblin awe cratered the diplomat's win-rate until the tactical-pacifist bot was taught the retreat-then-talk rhythm; a mechanic that changes what a route can DO but leaves its bot ignorant of the new move measures the OLD player against the NEW game — the same Goodhart the batch-16 relational flip gate polices, one layer down. Corollaries: bot-teaching is priced INSIDE the mechanic's batch (never a follow-on), and "the flip is expected to survive — measure it" is the mandatory sign-off line on any band-moving cast/combat change. Every diplomacy minigame the §9-F manifest still carries is a fresh instance of this trap.
- **`--render-frame --seed N [--ascii]`** (backend-term builds only): pure cell-grid → ANSI-escape byte stream written to stdout, no TTY required (safe with stdout redirected to a file) — this is both the frame-golden capture command and the terminal encoder's verification surface. Frozen by `tests/golden/frame_seed_1.bin`, `frame_seed_42.bin`, `frame_seed_1_ascii.bin`.
- **`--daily`**: shared seed of the day (`h64(days_since_epoch, ["daily"])`). Precedence: `--seed` > `--daily` > launch entropy.
- **`--replay <file>`**: replays a save headlessly and prints an FNV state hash + summary JSON. Two replays of one save must hash identically — if not, channel discipline broke. **`--load <file>`** resumes a save in the window (replays headlessly first).
- **`make check`** runs the whole gate in order: build, test, test-term, goldens, frames, xhash, solve, sim, **flip**, size. The **flip** step (`--sim-flip`, batch 16) is the mercy arc's central thesis as a make-check-owned invariant — `diplomat_win% ≥ violent_win% + margin` (margin 3 pts at N=5000, in `tests/tactical-pacifist-band.json`'s `flip_margin`/`flip_n`) — so the flip can't silently invert even though the per-policy bands now overlap. See the tactical-bands bullet below and the arc doc's "Ratification (2026-07-25)" section for why the RELATION (not an absolute win-rate) is the ratified thesis, and why it's a CONSERVATIVE proxy. **`make targets`** prints the size scoreboard (stripped/packed/% of budget, per backend — each backend's packed size is checked against the same 1,474,560-byte budget independently). **`make xhash`** is the cross-backend determinism gate: replay the committed fixture `tests/fixtures/ref.sav` through both backend binaries and require an identical state hash — it compares the two backends to each other (not to a frozen constant), which is the actual proof that the core/crust seam is real and neither backend's plumbing perturbs replay. `tests/fixtures/ref.sav` was regenerated in batch 9 T3 (seed 4242, a short overworld→dungeon session: S, E, E, E crosses screen 1's hole, then wait, S in the dungeon) since `replay()`'s front door changed to `Game::new_overworld` — the old pre-overworld fixture would have replayed as meaningless overworld wander under the new entry point. This was a fixture regen, not a worldgen MAJOR (see the Seed compatibility section and the batch-9 status log entry): `xhash` shifted (`3bb93aae84ab61fb` → `5f4d94c2f9e6aaca`, both backends agreeing on the new value) exactly as expected whenever hashed state or the replay entry point changes, while every dungeon-side gate (goldens, frames, solve, sim) stayed byte-identical.
- Any new system (new gen algorithm, new entity type, new item) should be observable via `--dump` or a new headless flag. Extend headless modes rather than claiming "it probably works."
- Sanity checks worth running after gen changes: `--solve 10000` green; every depth has one `<` at the entrance; depth 1–4 have exactly one `>`; depth 5 has the amulet and no `>`. (In dumps the player `@` sits on top of the entrance `<`.)
- Logic that can't be seen in a dump (combat math, FOV symmetry) should get `#[test]` functions. `cargo test` must stay green. Tests are free — they don't ship in the release binary.
- Window-path changes (input handling, frame pacing, minifb API usage) can only be compile-checked headlessly. Flag them explicitly in your summary as "needs interactive playtest" — do not report them as verified.

## Seed compatibility (MAJOR-version doctrine)

Worldgen output is a public API, frozen by the golden fixtures in `tests/golden/` (full 5-depth dumps for seeds 1, 2, 3, 42, 1337; `cargo test` string-compares them). Any change that diffs a golden **map layout** — the channel hash constants, the tag scheme, the order or count of draws on a worldgen/spawns/vault/theme channel, room/corridor/placement logic — breaks every seed and save in the wild. That is a MAJOR version bump requiring explicit human sign-off, never a drive-by. To re-baseline after sign-off: regenerate with `--dump --seed N > tests/golden/seed_N.txt`, re-run `--solve 10000`, re-commit `tests/solver-band.json`, and re-derive `START_LIGHT` from the new worst-case budget (its comment documents the derivation — keep it current). Dump-format-only diffs (headers, flavor lines) are not seed-breaking but still need the goldens regenerated; say so in the commit message.

## Code conventions

- **All randomness flows through named channels.** `channel(seed, &[tags])` (FNV-1a-64 + finalizer → xorshift64 `Rng`) isolates streams: `worldgen`/`spawns`/`vault`/`theme` are keyed per depth and frozen by the goldens; `combat`, `ai`, `flavor`, and `parley` (batch 5, the talk-receptivity roll) are per-run streams on `Game`. Never let one channel's draws leak into another — combat rolls must not perturb worldgen (there's a test). Never call system entropy mid-game; the only entropy is the launch-time seed.
- Coordinates are `i32`, grid indices via `idx(x, y)`, bounds via `in_map(x, y)`. Don't introduce a second coordinate convention.
- Entity/item stats live in the `Monster::stats`-style const match tables, not scattered magic numbers. New content = new table row.
- Turn structure: player acts (burning light via `spend_turn`) → `monsters_act()` → `compute_fov()`. Preserve this ordering; monsters must never act on a level the player just arrived on (see the early `return`s after stair transitions — they're intentional).
- Light is the run's clock: 1 per turn, 2 while carrying the amulet; 0 = death in the dark, and the lose check runs **before** the win check. `START_LIGHT` is solver-derived — its comment documents the derivation; don't retune it by feel.
- The win is a round trip: the amulet is picked up on depth 5 and carried to depth 1's `<`. Visited depths persist via `LevelState` snapshots — the climb out is through the world the player left. Don't regenerate visited levels.
- Bumping a wall costs no turn and burns no light (intentional). Pickup is on walk-over (intentional for v0; an inventory is a v1 discussion, not a drive-by refactor).
- Flavor is grounded: theme lore/adjectives may only restate things the engine did — never invent entities, exits, or events. Themes are const tables (`THEMES`); vaults are const strings (`VAULTS`) with the legend documented at the definition.
- Saves are seed + input log (`RL14` header format in `save_bytes`), never serialized world state. Anything that makes replay diverge from live play is a bug by definition.
- **The 80×30 cell grid is engine API; the window is presentation.** `COLS`/`MAP_H` are baked into `idx()` and worldgen — the grid must never follow the window size. Frontends scale the fixed 640×360 buffer (minifb: `resize` + `AspectRatioStretch`); a DOS/mobile port swaps the backend module, not the grid. Likewise the input-byte vocabulary is the platform boundary: any frontend that produces those bytes is a valid client. It is now **0–16** (batch 8 T1 added byte 16): 0–3 move/bump-attack, 4 wait, 5 restart (reroll to a new seed, `INPUT_RESTART`), 6 retry-same-seed (`INPUT_RETRY`, batch 4/save v2), 7–10 talk-N/S/W/E (batch 5, byte order mirrors the move bytes), 11–14 give-N/S/W/E (batch 7 T2, byte order mirrors move/talk), 15 use (batch 7 T2, self-apply the top of `Game.held`, no direction), 16 put-down (batch 8 T1, story §9-D, `Game::put_down` — self-apply like 15, no direction, sets the carried objective back into the current tile's item list and its carry-burn multiplier reverts to base automatically via `spend_turn`) — 5 and 6 are reconstruction bytes handled in `replay()`/the backend layer, not ordinary `apply_input` moves; 7–10, 11–14, and 16 are ordinary `apply_input` moves that route to `try_talk_player`/`try_give_player`/`put_down` instead of a bump-attack. A frontend turns a `t`(or `a`)+direction chord into one logged talk byte, `g`+direction into one logged give byte, a bare `u` into byte 15, and a bare `p` (no chord, batch 8 T1) into byte 16; the armed/disarmed chord state itself is frontend-local chrome, never engine state (same status as the F5 double-press save-overwrite confirm) — it's why the chord-reading helpers live in the backends, not `game.rs`. The title screen documents all of these ("Give: g+dir    Use: u    Put down: p") — a batch-7 T2 finding was that this legend line went in without the fix landing in the same commit; it's fixed now (`a249dd2`), and batch 8 T1 extended the same line with the put-down key rather than repeating the omission. **Batch 15 T2 added a use-by-kind range on top of 0–16: bytes `17 + kind` = "use one held item of item-kind `kind`"** (17..`17 + GAME.items.len()`), routing to `Game::use_item_kind`, so a player can pick WHICH held item to use instead of only the LIFO top (byte 15 stays as use-top). The `u` key now opens a frontend use-selector — a one-line menu of `Game::held_summary()` (held kinds + counts, e.g. `Use: 1:o x2  2:~ x1`, named by GLYPH so the backends stay grep-clean) whose digit keys emit `17 + kind`; the selector's armed state is frontend-local chrome exactly like the talk/give chords. This is the first vocabulary growth past 16 and drove the `SAVE_VERSION` 9→10 bump (an old binary must reject a save whose log contains a `17+` byte).
- Saves are `SAVE_VERSION 10` (`save.rs`): batch 15 T2 bumped 9→10 when the use-by-kind input bytes (`17 + kind`) grew the vocabulary; batch 15 T1 bumped 8→9 when `Monster.struck_player`/`Monster.yielded` (the goblin-awe playtest fix) joined the hashed per-monster state; batch 14 T3's bump to 8 added `Game.cache_light_collected`; batch 13 T3's to 7 added `Monster.dividend_paid`; batch 12 R4's to 6 added `mood_sum`/`mood_count`. `parse_save` accepts version bytes `1..=10` and replays each byte-identically (a v1 log can never contain byte 6, a v1/v2 log bytes 7–10, a v1/v2/v3 log bytes 11–15, a v1..=4 log byte 16, a v1..=9 log a `17+` use-by-kind byte, and NO version's log contains a byte for the mood/dividend/cache/struck/yielded mechanics — they're pure derived-or-per-monster state with no input entry — so accepting older versions changes nothing about their replay). **Note the batch-12/13 bumps are NOT vocabulary growth** — no new input byte landed (the mood system rides existing move/wait/pickup bytes); the version bumped because the *hashed state shape* changed (`mood_sum`/`mood_count` joined `state_hash`), so an old binary must reject a v6 save rather than replay it under the old hash and silently diverge. Both reasons — vocabulary growth AND hashed-state-shape change — are version-bump triggers. The version bump's own point is the opposite direction: an *old* binary handed a newer-version save must reject it cleanly rather than silently ignoring what it doesn't know and diverging from the log that produced it. Don't drop old-version acceptance, and don't skip a version bump when either the vocabulary or the hashed-state shape grows again.
- **Held items are a LIFO list, not an inventory UI** (`Game.held: Vec<u8>`, batch 7 T2, story §9-A — "a small held-items list, hashed, no grid UI"). `ItemDef::on_pickup` is `Hold` (pushed onto `held` on walk-over) or `Consume` (the original v0/v1 instant-apply-and-vanish behavior, still used by the sword/amulet/lore items) — a per-item authored choice, not a global switch. GIVE (bytes 11–14, `Game::try_give_player`) and USE (byte 15, `Game::use_item`) both act on `held.last()` only: GIVE consults `GameDef::give_table` for a `(item, target monster kind)` row (regard delta, optional heal-to-full, optional line, whether the give consumes the item) and no-ops gracefully with a `StringsDef` feedback line if there's no monster in the offered direction, nothing held, or no table row for that pair; USE applies `ItemDef::on_use` (`Heal`/`Light`, batch 7's only two variants) to the player and no-ops with its own feedback line if nothing's held or the held item has no self-use. Both are ordinary turns (`spend_turn`, no violence tax — offering or using an item isn't violence) and both pop `held` on a landed effect. `held`'s length and contents are hashed (`state_hash`) — order matters for what GIVE/USE will act on next, so it's run-defining state, not presentation. This batch moved the **potion** from walk-over `Consume` (instant heal) to `Hold`+USE — the greedy sim bot was updated to emit USE when hurt and holding a potion (a real bug was caught and fixed here: an earlier bot check used `held.contains()` instead of `held.last()`, which could fire USE on a potion buried under another held item that GIVE/USE can't actually reach — fixed before the sim numbers below were taken as the baseline).
- **The McGuffin talks through `CarryEvent` dispatch** (batch 8 T1, story §9-B/C/D — "voice two comes online"). `GameDef::carried_lines: &[(CarryEvent, &[&str])]` is a generic engine-side hook — an event tag to line-pool table — plus `GameDef::carried_preamble: &[&str]`, a fixed intro sequence. `Game::carry_event(ev)` fires ONLY while the objective is being carried; if `carried_lines` has no row for `ev`, or that row's pool is empty, it's a provable no-op (no RNG draw, no mutation) — the invariant that kept T1 byte-identical to pre-batch-8 with an empty-pool cartridge, and the same invariant that lets a future cartridge ship zero McGuffin content for free. Line selection draws from `flavor_rng` only, never `combat`/`ai`/`parley`/worldgen channels. **§9-C, the pickup register**: at first pickup, `CarryEvent::PickedUpBloody` fires if `kills > spared`, else `PickedUpMerciful` — preceded, once, by `carried_preamble` printed in fixed order (never shuffled, never repeated on a later pickup). A re-pickup after a `put_down` fires `CarryEvent::PickedBackUp` instead. **The climb ladder**: `CarryEvent::StairsUp` is dispatched on every carried ascent, indexed by `Game::speech_attempts` (a hashed `u8`) rather than drawn at random — the index is read BEFORE the counter increments, so the first carried ascent reads rung 0, matching an ordered "one true sequence" pool rather than a shuffleable one. **New hashed state**: `speech_attempts` and `objective_dropped` (both run-defining — they change which `CarryEvent` a future dispatch fires — so both join `state_hash`, same status as `held`/`regard`/`calm`/`spared`); `Game::mcguffin_last_line_turn` (the rate-limit clock, at most one line per `carried_lines`' configured turn gap except the always-speak events `PickedUpBloody`/`PickedUpMerciful`/`PutDown`) is presentation-only and lives in the `killer`/`echo`/`facing`/`fx_hit` exclusion set instead (see that doctrine above). **Put-down** (byte 16, `Game::put_down`, story §9-D) is legal anywhere the objective is carried: it re-enters the objective into the current tile's item list, flips `objective_dropped`, fires `CarryEvent::PutDown`, and the carry-burn light multiplier reverts to base automatically through the existing `spend_turn` path (no separate revert logic needed — carry-burn was already conditional on `has_objective`). A found-during-review gap: the brief had assumed §9-C was "just T2 data," but the pickup-register branch needed an engine hook of its own (the kills-vs-spared read at first-pickup time) — folded into T1 as a fix round rather than punted to T2, since T2 owns only cartridge data, not engine logic.
- **The overworld is a 3-screen `WorldId::Overworld` world, reusing `descend`/`ascend`'s own machinery** (batch 9, story §9-J prep). `WorldId::Overworld` is a unit variant; `Game::depth` (1..=3) is reused as "current screen index" exactly the way a `Floor` world already pins `depth = 1` — so `Game::stash_level`/`restore_level_at`/the per-slot `saved` stash need ZERO changes to also cover the overworld's 3 screens (`Game::new_overworld` just sizes `saved` to 3 instead of `GAME.win.max_depth`). The 3 screens are fully authored `&'static str` ASCII (`GameDef::overworld: OverworldDef`, zero RNG, same convention as `AUTHORED_FLOORS`), parsed by `Game::instantiate_overworld_screen` — so this is NOT a worldgen MAJOR (see Seed compatibility below): `Game::new`/`gen_level`/the worldgen/spawns/vault/theme channels are untouched, and all 5 dungeon dump goldens plus both frame goldens stay byte-identical. Three new tiles: `=` (`Tile::ScreenLink(bool)`, dump glyph `=`) is a DETERMINISTIC screen-to-screen link — distinct from the random `*` portal — walk-onto crosses instantly via `Game::cross_screen_link` (a near-verbatim copy of `descend`'s shape), never a describe-then-wait like a portal; the bool says which edge (east/next vs. west/prev), derived once at parse time from which edge column the glyph sits on. `V` (`Tile::Hole`) is the overworld's one true engine-plumbing tile: walk-onto transits from `WorldId::Overworld` into the ROOT dungeon (`WorldId::Seed(seed)`) via `Game::cross_into_dungeon`, reusing the SAME portal forward-transit machinery (`enter_world_forward`) with a statically-known destination instead of a rolled one — this is why the existing win-check (`land_on_tile`'s `UpStairs` arm, gated on `self.world != WorldId::Seed(self.seed)`) needed zero changes: entering the hole makes that comparison true exactly as a direct `Game::new` start always has. `+` (`Tile::ShutDoor`) is a dumb, always-impassable door this batch (always logs a fixed decline message regardless of `has_objective`) — SPACES-DRAFT-v0.md flags a future state-dependent "opens once the McGuffin is held" behavior as the §9-I mantel/ending sequence, explicitly NOT built yet. `MonsterDef` gained `passive: bool` (checked in `monsters_act` beside the existing `calm` skip — gates AI only, from spawn, no chase/attack ever) and `bump: BumpResponse` (`{Fight, Yield, Shove}`, default `Fight` = today's bump-attack unchanged) — `TRAINER` (`passive: true, bump: Yield`, swaps position like a becalmed monster) and `DONKEY` (`passive: true, bump: Shove`, pushes one tile via the sokoban destination check or plants if the destination isn't plain floor) are the two new un-killable-by-construction rows, placed by glyph (`Y`/`D`) exactly like `AuthoredFloorDef` places monsters — no new entity-kind engine surface. `spend_turn` gained one exemption: no light deduction while `self.world == WorldId::Overworld` (still increments `turns`), so waiting in the overworld can never kill you in the dark — the torch clock is a dungeon-only mechanic. `Game::new(seed)` itself is UNCHANGED (still "start directly in root dungeon depth 1," used by every headless verification surface); `Game::new_overworld(seed)` is the new sibling and, as of batch 9 T3, the REAL interactive/replay front door — see the Verification section above for exactly which call sites use which. SAVE_VERSION stays 5 (no input-byte vocabulary change this batch — screen-links/holes fire on ordinary walk-onto, not a new byte).
- **Ghost files** (`rl144-<worldhash>.ghost`, `RLG1` magic) are the death-capture sibling of a `.sav`: written by pure builders (`ghost_bytes`/`parse_ghost`) in `save.rs`, zero I/O in that module — the backends own the actual file write, same core/crust split as everything else. Auto-written on death when the End screen's R/N/Q is pressed (never on a win); latest death overwrites, one ghost per world. Labels come only from the const `GHOST_LABELS` table via `content::ghost_label_idx` (deterministic from outcome/depth, no new RNG channel, no free text ever — same discipline as flavor/lore). Phase 1 substrate only: no ghost rendering or playback yet, so `Ghost`/`parse_ghost` are `#[allow(dead_code)]` pending that later task.
- **Violence tax** (`VIOLENCE_TAX` in `game.rs`, batch 4, DECISION.md item 1 — the mercy economy's first brick): every bump-attack burns 1 extra light on top of the normal per-turn cost, folded into `spend_turn`'s single deduction so the light-0 death check still runs exactly once and lose-before-win ordering holds — a killing blow whose tax lands light at or below 0 is correctly a darkness death. Wall bumps still cost no turn and burn no light. The tax stands unchanged through batch 5 — mercy is priced on top of it, not instead of it.
- **Mercy is a verb and the verb is talk** (batch 5, DECISION.md item 3, the Henson ruling — "if you could talk to a rat, you could give a rat mercy"). A talk byte (7–10, directional) at a monster rolls its receptivity — the full formula with per-term provenance lives at `receptivity()` in `game.rs`; reference it there, don't duplicate the math in comments or docs. A landed roll: `+1 regard` and the monster is stayed this turn (it's listening, so it doesn't get to swing — implemented as a transient per-turn parameter threaded through `monsters_act`, never stored, never hashed, cleared every turn; other monsters act normally, so crowds stay dangerous). A failed roll: no regard, and the monster is *not* stayed — it acts normally, meaning it can retaliate the same turn. Talking always costs the turn either way, tax-free; talking carries risk precisely because failure buys nothing. A monster becalms (`Monster.calm`, `Game::spared` increments) at `Monster::talk_threshold(kind)` — rat 2, goblin 3, ogre 4, unchanged since batch 5 T1. A becalmed monster never attacks or chases and yields (position-swaps, no damage, no tax) on a bump instead of being attacked. `regard`, `calm`, and `spared` are all hashed state (`state_hash`) — mercy is run-defining, not presentational; the existing `killer`/`echo`/`facing`/`fx_hit` exclusion set is unchanged. Talk rolls come from the `parley` channel (`channel(seed, &["parley"])`), a new per-run stream on `Game` alongside `combat`/`ai`/`flavor` — never `combat_rng`, never worldgen.
- **The complete ogre: fight it (costly) or stand tall (awe it)** (batch 11, per `docs/design/2026-07-22-mercy-economy-arc.md`'s "the complete ogre"). The ogre's guaranteed hit and its awe-becalm are the same coin — the hit IS the awe test — and both are cartridge-data-driven so the engine stays grep-clean (it reads `Monster::stats(kind).retaliation`/`.awe_threshold` generically; ogre-ness lives only in `contractor.rs`). **`MonsterDef.retaliation: i32`** (0 for every kind but the ogre, currently 6): the instant the player bump-ATTACKS such a kind it lands a guaranteed hit back — even a killing blow costs HP. It's folded into the attack branch and then falls through to `spend_turn`, so a lethal retaliation advances `turns`/`light` like every other death path, with `killer` staying the attacker on a same-turn HP+light tie. **`MonsterDef.awe_threshold: u8`** (0 = not awe-able; ogre 3) + the **hashed `Monster.awe: u8`**: *standing tall* — ending your turn cardinally adjacent to the ogre, without fleeing and without swinging — builds `awe`; at the threshold it becalms via the SAME `calm`/`spared` path a talk-becalm uses (so all downstream mercy behavior is unchanged). Fleeing or attacking resets `awe` to 0. **Load-bearing subtlety:** "held ground vs. fled" is decided from a PRE-chase snapshot of monster positions (the single `monsters_act_and_resolve_awe` helper all turn-advancing sites route through), measured against the player's OWN move — NOT post-`monsters_act` adjacency, or a straight-line retreat from a same-speed chaser would awe the ogre for free (the bug T2's fix closed). `Monster.awe` is run-defining → hashed in `state_hash` beside `regard`/`calm` (NOT presentation-only); `retaliation`/`awe_threshold` are `MonsterDef` constants, not hashed. The diplomat's answer to an ogre is its EXISTING cornered-talk — talk is a no-move action, so it holds ground and awes automatically; no sim-bot "wait" branch was needed (and a bare wait would risk portal-transit). Heal scarcity (the batch-11 balance MAJOR) makes enduring the awe cost real HP; batch 12's light-as-grace is what pays that cost back (spare → light), which is why at batch 11 the mercy route is *harder* than violence, by design.
- **Light as grace: the McGuffin is a second, positional light source whose radius is her verdict on you** (batch 12, per `docs/design/2026-07-22-mercy-economy-arc.md`'s "Engine 2 — light as grace", amended mid-arc to the human-delivered "second-lantern" spec — this is the batch that FLIPS diplomacy above violence). The mechanism has four coupled parts, all cartridge-data-driven so the engine stays grep-clean: **(1) The death condition was rewritten.** `light <= 0` no longer kills outright — the darkness death fires only `if self.light <= 0 && !self.lit_by_mcguffin()` (`game.rs`, lose-check still BEFORE the win-check). She is literally a second lantern: `Game::mcguffin_light() -> Option<((i32,i32), i32)>` returns her tile + shine radius (`None` when the radius is 0), and `compute_fov` composes the torch circle and her circle through the shared `light_circle` raycaster, so a merciful player whose torch has died can still finish the climb inside HER light. **(2) Her radius is her mood.** `mood_shine_radius(mood)` maps `Game::mood()` (0–100) through `BalanceDef::mood_shine_tiers` `[(75,6),(50,4),(25,2),(i32::MIN,0)]` — the dark tier is pinned to 0 so a mood-0 brute gets NO shine and dies in the dark exactly as before batch 12 (this is the whole flip: mercy lights the way home, violence doesn't). **(3) Mood is a running average anchored at pickup.** At the FIRST pickup (`Game::pickup`) the mood is SEEDED from the descent's kill/spare record — `anchor_score = 50` if you never picked a side, else scaled toward `0` (pure brute) or `100` (pure diplomat) — as `mood_sum = mood_anchor_weight * anchor_score; mood_count = mood_anchor_weight` (`mood_anchor_weight = 20`, so the anchor is worth 20 acts, giving the descent real inertia). `Game::mood()` is then `(mood_sum / mood_count).clamp(0,100)`, a running average: every post-pickup kill averages in `MonsterDef::kill_valence` (graduated — rat 0, goblin 15, ogre 30: not every kill is equally despicable) and every post-pickup spare/becalm averages in `mood_spare_valence` (100). Both `mood_sum` and `mood_count` are hashed (`state_hash`) — mood is run-defining, so it drove the `SAVE_VERSION` 5→6 bump (NOT a vocabulary change; a hashed-state-shape change). **(4) The HP economy it pays back.** Batch 11 made heals scarce; batch 12 R3 added REST — a wait (byte 4) while hurt and NOT Chebyshev-adjacent to a live non-calm monster heals `BalanceDef::rest_heal` (1) HP (`Game::rest_heal`, gated so it never doubles with the ogre stand-tall/awe turn). A kill ALSO dims the torch directly (`BalanceDef::kill_light_penalty`, T1). The McGuffin comments on a mending rest with mood-keyed lines (R7: `CarryEvent::RestedBright` when her ring is wide, `RestedDim` when she's gone dim but the mending doesn't need her). **The measured flip** (`--sim 5000`): the diplomat (`tactical-pacifist`) wins **59.7%**, ABOVE the violent fighter (`tactical`) at **43.7%** — because the diplomat's high mood shines the climb home (`deaths_dark` 14) while the brute's dark McGuffin leaves him to die (`deaths_dark` 762). **Solver conservatism:** her shine only ever ADDS light, and `--solve` ignores her entirely (it never routes through a pickup-and-carry), so the winnability lower bound stays valid and unmoved. **NOT a worldgen MAJOR:** all dump+frame goldens are byte-identical (no channel/layout touched); the only expected shifts are the four re-baselined sim bands (R6) and `xhash` (SAVE_VERSION 6 + `mood_sum`/`mood_count` joining the hash, both backends agreeing).
- **The becalm toolkit has texture: read the creature, use the right mercy** (batch 13, the mercy-economy "ride-alongs" per `docs/design/2026-07-22-mercy-economy-arc.md`; all cartridge-data-driven, engine grep-clean). Diplomacy stops being spam-talk and becomes per-enemy nerve. **Goblinoid awe** (`MonsterDef::awe_by_giving_ground`/`punish_wrong_move`, `Game::resolve_awe`): the goblin is the MIRROR of the ogre. The ogre becalms by HOLDING (stand adjacent through its hit); the goblin becalms by GIVING GROUND (was toe-to-toe, stepped away — "walk backward while talking"). Each PUNISHES the opposite move with a hit — plant yourself against a goblin (`punish_holding`) or flee an adjacent ogre (`punish_flight`) and it lands the monster's own swing on you (reusing the failed-talk/enrage hit path, `killer` attributed on a lethal one). The awe read reuses batch 11's exact PRE-CHASE-snapshot discipline (`monsters_act_and_resolve_awe`'s `pre_chase`), so a straight-line retreat that the goblin chases back to adjacency still counts as giving ground (guarded by the `straight_retreat_from_goblin_builds_awe_despite_post_chase_adjacency` probe test). `held_adjacent` requires `old_dist == 1` AND the player actually STOOD STILL (`(px,py) == prev_player`) — a fresh approach or ordinary corridor traffic near a goblin is a neutral non-event, not "planted" (two real bugs the T5 sim measurement caught). `Monster.awe` is already hashed; no new hashed field. **Potion is mammal-medicine** (`GiveRule::enrage`, per-kind rows): human medicine heals mammals and poisons everything else — potion→rat heals-to-full + becalms (the most self-sacrificing mercy), potion→goblin/ogre ENRAGES (regard crash + a free swing at you via the failed-talk hit, NO damage to the monster, not tied to player ATK — a pure read-the-bestiary trap, never a ranged-violence loophole). The old blanket potion→any row is gone. **Cheese → goblin** (`GiveRule::stay_and_roll`): the human-assigned positive cheese target (story §12.14) — cheese ALWAYS stays the goblin (guaranteed tempo, reusing the batch-5 transient stay via `monsters_act_and_resolve_awe(Some(mi), …)`) AND rolls `receptivity` off `parley_rng` for an outright becalm (gambled grace). Cheese→rat stays the batch-7 PENALTY; ogres get no cheese route. **Becalm return-trip dividend** (`BalanceDef::becalm_dividend`, hashed `Monster.dividend_paid`): passing a becalmed monster refunds a trickle of light once per monster ("it lights your way home") — kept SMALL (currently 2, a flavor touch, not a balance lever; the arc GUARD is that this is the exact knob that made pacifism dominant in batch 5). **The trainer reads your last life** (batch 13 T1, presentation-only — see the exclusion-set doctrine above): resurrection is the only way back to him, so his greeting is inherently ironic (bloody return → `TRA_007` salutes the rat-slaying that killed you; merciful → a wary counterpart). **The donkey-follow seed** (`MonsterDef::follows_when_calm`, rung 2 of the arc's three-rung ladder): schmooze the donkey to its top `DONKEY_TALK` rung (calm) and it trails you around the overworld and across screen-links (`Game::take_calm_follower`), but WILL NOT descend the hole — "he only goes down for someone he loves." Rung 3 (LOVE → descend → ogre-becalm-by-presence → the secretly-super reveal) is a later companion batch on the §9-F NPC-vault MAJOR, NOT built. **Combat-balance sign-off class** (like batch 11, NOT a worldgen MAJOR): goldens+frames byte-identical, all four sim bands re-baselined (T7). The measured landing: **diplomat 51.0% > violent 33.9%** — the flip HELD through the harder diplomacy (goblin awe made it a skill; T7's single measured lever, goblin `awe_threshold` 3→2, lifted the diplomat back into the arc's 50-60 target). Dumb play still loses (<2%), competent violence is doomed (~34%), diplomacy is the reliable road. `SAVE_VERSION` 6→7 (`dividend_paid`), `xhash` shifted at T3 to `ca6604617a55903a`.
- **Portal ROI: a portal's authored-floor destination holds a light-cache reward, telegraphed at the threshold** (batch 14, the arc doc's original item-4 "portal ROI"; human sign-off 2026-07-24 for the authored-floors MVP). Portals were a curiosity with no payoff; now they're a light-pilgrimage the light-rich (merciful) player can afford and the slaughter-route player can't. **The reward** is `ItemEffect::LightCache(i32)` (`LIGHT_CACHE`, glyph `$`) — a walk-over `Consume` item that refills the run's light pool, placed in the two `AUTHORED_FLOORS` a `Dest::Floor` portal leads to (NEVER in a root-world depth, so root goldens are untouched). **The value is DERIVED, not hand-picked** (`--probe-floors`): the geometric dive cost = `2 × bfs_dist(floor entry `<` → the cache)` (round trip, 1 light/turn); the cache value = the worse floor's cost + a documented margin (21 = 14 + 50%), so a dive is +EV — documented `START_LIGHT`-style. **The anti-brute-bailout guard** (`BalanceDef::max_cache_light_per_run = 60`, hashed `Game.cache_light_collected` → `SAVE_VERSION` 7→8): caps total cache light per run WELL below what a slaughter route self-burns (`kill_light_penalty 8 × ~17 kills/run ≈ 135`), so caches offset only a fraction of a brute's self-inflicted loss while a merciful player (no kills) keeps the full benefit; doesn't bind today (2 caches × 21 = 42 < 60) — a forward guard. **The telegraph is grounded against actual presence** (`Game::portal_describe` → `map_has_cache_reward`): the `Dest::Floor` describe line previews a cache ONLY when that floor's authored map actually contains one (reads the cache glyph from the item def, engine stays grep-clean), never promising a cache a barren floor won't deliver. **NOT a worldgen MAJOR** and structurally orthogonal to balance: root dump+frame goldens byte-identical, `--solve 10000` unchanged, all four sim bands UNCHANGED (bots never transit portals — transit needs an explicit wait no bot emits — so portal ROI is human-facing exploration content, NOT a sim-measured lever). The Dest::World multiverse enrichment (procedural destinations' loot) is a pre-approved follow-on needing its OWN worldgen MAJOR sign-off with this batch's probe data in hand; no portal-diving bot yet.
- **Goblin awe is earned by talking while giving ground, and dies once the goblin has struck you** (batch 15 T1, a human playtest fix refining the batch-13 goblin awe). The batch-13 version awed a goblin on a bare SILENT give-ground move — wrong: a goblin you're just fleeing, or one that already hit you, shouldn't be pacified. Now: a give-ground move only ARMS `Monster.yielded` (hashed); the awe BUILDS on a subsequent TALK at the goblin (`resolve_awe` gains a `talked` param) when `yielded && !struck_player`, consuming `yielded` — the two-beat "retreat, then talk" rhythm the arc's "walk backward while talking" always meant. `Monster.struck_player` (hashed, set the instant ANY monster lands a hit) PERMANENTLY blocks give-ground awe — a goblin that has struck you can only be fought, fled, or cheesed, never awed. And talking a goblin is composure, NOT planting: a talk turn is excluded from `held_adjacent`, so it isn't punished (without this the mechanic is impossible — a talk is a no-move, which otherwise reads as "planted"). **The ogre is untouched** (`struck_player` gates only give-ground kinds — enduring the ogre's hit IS its awe). Both fields are run-defining → `state_hash` + `SAVE_VERSION` 8→9. **Combat-band-moving** (like batch 11/13): the tactical-diplomat bot learned the retreat-then-talk rhythm, and the flip HELD but NARROWED — diplomat 51.0%→**36.2%**, violent unchanged at **32.3%** (it never talks/gives ground), greedy/pacifist floors unmoved, all stuck=0. Bands re-baselined. **RESOLVED in batch 16** (see that bullet below and the arc doc's "Ratification (2026-07-25)"): the thin margin was addressed on two fronts — the goblin `awe_threshold` was lowered 2→1 (the surgical lever; diplomat 36.2%→~39%), and, more importantly, the thin margin exposed that the flip lived only in overlapping band positions, so the flip is now a make-check-owned RELATIONAL invariant (`--sim-flip`), not a hope the bands imply. The ratified metric is the relation (`diplomat > violent + margin`) PLUS the diplomat band's thesis-bearing floor, not an absolute win-rate — because the diplomat bot doesn't model cheese→goblin / potion→rat, so its number understates human diplomacy (a caught Goodhart, ratified through human sign-off).
- **The cast substrate: talk-minigame framework, guaranteed NPC-vault placement, sword-as-held, disguise, lesson-state** (batch 17, the mimic — cast batch one of the road-to-1.0 arc; all cartridge-data-driven, engine grep-clean). The durable surfaces future cast batches + the ending (§9-I) build on: **(1) Talk-minigame framework** — `MonsterDef::talk_minigame: Option<Minigame>` (`{Echo, AnswerSecondVoice, PoliteDecline}`), dispatched at `try_talk_player`'s receptivity seam. Load-bearing invariant (batch-8 empty-pool discipline): `None`/`Echo`/`AnswerSecondVoice` take the UNCHANGED flat roll — only `PoliteDecline` branches — so tagging a monster is behavior-neutral unless it's PoliteDecline. `PoliteDecline` = THE POLITE NO: talk (decline) always advances toward becalm; ending a turn adjacent WITHOUT declining costs `polite_decline_accept_damage`; attacking is rudeness. **The mantel exam (§9-I) reuses this framework verbatim** — the coat's `AnswerSecondVoice` (cast batch two) plugs in the same way. **(2) Guaranteed NPC-vault placement** — `GameDef::required_vaults: &[(u8, &str)]` + `place_required_vault_room` (an EXHAUSTIVE two-tier scan — a bounded retry is NOT a guarantee, it missed ~5% of seeds); a ROOT cast depth stamps its required vault unconditionally on the `vault` channel BEFORE the optional `vr.chance(2,5)`, other depths byte-for-byte unchanged (per-depth channel → non-cast goldens stable). Cast batch two adds D2 (coat)/D4 (lost guy) rows to this same table. THE STAGE (D5) holds the objective's `&` pedestal — the objective no longer pushes to the deepest-BFS room. **(3) Sword is `Hold`, ATK derived** — `recompute_atk` = `starting_atk` + Σ held `AtkBonus`, called at every `held` mutation (a set-down sword's bonus can't linger). **Set-down-for-regard** (`ItemDef::disarm_regard`) banks regard when the sword is set down adjacent to a monster, GUARDED once-per-monster by hashed `Monster.disarm_regard_paid` (the arc's cheap-regard GUARD — unguarded, set-down/pickup oscillation farms mass becalm, batch 5's dividend risk). **Put-down is kind-routed** (byte range `17+len(items)+kind` = set-down-by-kind, mirroring use-by-kind; byte 16 stays put-down-objective) — the batch-15-style LIFO ambiguity is gone. **(4) Disguise/ambush** — `MonsterDef::starts_disguised` (grep-clean flag) + hashed `Monster.disguised`: inert (no chase/attack) until Chebyshev-adjacent, then reveals PERMANENTLY (one-way) and behaves that same turn; glyph unchanged (behavioral disguise + a narrative reveal line, no glyph swap). **(5) Lesson-state** — hashed `Game.echo_done`/`answer_done`/`endure_done`, set on the respective becalm; **the mantel (§9-I) reads all three** to gate the TALK ending's curriculum payoff. `answer_done` is reserved (nothing sets it until the coat). `MonsterDef::climb_reencounter` fires a `CarryEvent` when a carrying player passes a becalmed such monster (the mimic's §3.5 re-encounter; McGuffin line is a marked `[YOURS]` placeholder). All landed with `SAVE_VERSION` 11 and the flip intact (+3.6) — see the batch-17 status entry for the difficulty-drift flag (the guaranteed rooms made the game easier: violence 32→40%).
- **Portals are doors to other worlds** (batch 6 T1, `Tile::Portal`, dump glyph `*`). Placed via the worldgen channel, ~1/4 per depth, never in the entrance or exit room. A portal's destination is rolled ONCE at generation time and cached (`Game::portal`/`LevelState::portal`), never re-derived: either `Dest::World(seed, whash)` — `seed = h64(world_seed, ["portal", depth_tag, index_tag])`, `whash` memoized via `headless::world_hash(seed)` — or `Dest::Floor(u8)` indexing `content::AUTHORED_FLOORS` (~1/3 of rolls when any floors exist). The whole multiverse is a pure function of the root seed, frozen by goldens like everything else. Walking ONTO a portal never transits — it only logs the destination (theme label + world hash, or the authored name), grounded because the engine proves it by generating the destination. Transit is pressing wait (byte 4) while STANDING on the portal — a deliberate reuse of the existing wait byte, so the input vocabulary doesn't grow and neither sim bot (which never emits wait) can transit by accident. Light, hp/maxhp/atk, kills, spared, and `has_amulet` are GLOBAL across worlds — the torch is the multiverse's one clock. The amulet exists only in the ROOT world's depth 5, and the win check fires only on the ROOT world's depth-1 `<`. In any non-root world, depth-1's `<` (or a `Floor`'s sole `<`) is the return portal: walk-on, like stairs, lands you back on the source world's portal tile. `Game::worlds` (a `WorldId -> WorldState` map, insertion-ordered by first-LEFT) holds every visited non-root world's saved `LevelState` stack plus its own provenance (`from`), so a multi-hop portal chain unwinds correctly; all of it is hashed in `state_hash`. Solver/sims are structurally unaffected: root-world BFS reachability never routes through a portal (transit needs an explicit wait), so `--solve` and both `--sim` bots ignore portals entirely — a portal-diving bot policy is future work, not this batch's.
- **Authored floors** (batch 6 T1, `content::AUTHORED_FLOORS`) are hand-built, singular, non-random single-level maps a portal's `Dest::Floor` can point to — no depths, no stairs, no amulet, just a `<` that returns to the source portal. Same floor reached from two different worlds is the SAME floor (keyed by `WorldId::Floor(index)`, not by which portal led there), so its visited state (kills, pickups) persists across every door onto it. This is the enabling tech for a future fixed-overworld screen (story `docs/story/STORY-COMPILE-v1.md` §9-J) — don't re-derive an equivalent mechanism elsewhere.
- **Sokoban** (batch 6 T2, ported in spirit from `golem/topdown-puzzle`'s `shared/push.js`, vault-only): pushing a block (`Game::blocks`, walk-into) costs a turn of light like any move; a push chain is capped at `MAX_PUSH_CHAIN = 2` (a 3rd consecutive block refuses the whole push, no turn spent). A chain's farthest member landing in a `Tile::Pit` is destroyed and the pit FILLS (`Tile::Floor`) — the sokoban bridge; landing on a `Tile::Goal` LOCKS it there instead (absorbed out of `Game::blocks`, tile reverts to `Tile::Floor`) per `docs/story/STORY-COMPILE-v1.md` §9-H ("a block pushed onto a goal locks and opens/reveals the room's reward"). Blocks are hashed state (position matters) and persist per seed — "someone was here, someone was bad at this." A pit is impassable to player and monster alike (refused like a wall, no turn); a goal tile is ordinary floor for every game-logic purpose except the push-lock and its distinct render color. Abandoning a sokoban room mid-solve is always legal — these rooms gate loot only, never the level exit (`Game::gen_level` excludes a sokoban vault's own center from exit-room selection, and the solver proves the exit stays reachable on every seed regardless).
- **`docs/story/STORY-COMPILE-v1.md` is DESIGN CANON** (adopted batch 6, `035e68f`). Batch 7+ implements its §9 asks in order (A first, per the doc); any new mechanic must be priced there before landing (Rule 4: "new mechanics are priced, never assumed") — check it before proposing new systems, the same way `tests/solver-band.json` gates worldgen. **§9 checklist status**: A — done, batch 7 (give/use verb, cheese/coat/towel). H — done, batch 6 (sokoban push/pit/goal). B/C/D — done, batch 8 (`docs/superpowers/plans/2026-07-19-batch-8-mcguffin.md`: the McGuffin's voice — event lines, the kill/spare-keyed pickup register, the put-down verb, `CarryEvent` engine hooks). J — done, batch 9 (`docs/superpowers/plans/2026-07-21-batch-9-overworld.md`: the overworld skeleton — 3 fixed screens, `=` screen-link, `V` hole into the dungeon, minimal un-killable trainer/donkey cast, `Game::new_overworld` as the real front door); J's own corpse/loot-persistence-across-death-retry half (SIGN-OFF ASK #7) is explicitly NOT done, priced as a separate future mechanism. **E — done, batch 12** (light-as-grace, the second-lantern design — the McGuffin's shine radius = f(mood), the flip that put diplomacy above violence; `docs/superpowers/plans/2026-07-24-batch-12-light-as-grace.md`). F (the coat/mimic/lost-guy dungeon cast + minigames — needs DECISION.md item 4's NPC-vault worldgen MAJOR, a materially bigger ask than J's `passive` flag was), G, I (the mantel/ending sequence behind `+`'s dumb-this-batch ShutDoor), K (donkey re-glyph) are not yet briefed — write each one's brief from the story doc's own §9 entry when its turn comes, don't guess ahead; §9-E is explicitly flagged in the batch-8 brief as NOT that batch's scope (cheap mechanically, but tunable-heavy, deferred to batch 9+ — still true after batch 9, which spent its budget on J instead). Story Rule 4 also means batch 7 shipped nothing beyond §9-A even though the give-table's shape supports more (see COAT/TOWEL's no-give-target rows in `contractor.rs` — items exist, their give-targets don't yet, because those targets' monsters aren't priced/built), batch 8 T2 held out five NAR_ lines and two MCG_ lines for the same reason, and batch 9's `OVERWORLD_1`/`OVERWORLD_2`/`OVERWORLD_3` map/name/describe strings in `contractor.rs` are still MINIMAL VALID PLACEHOLDER content (bordered, legal legend, correctly linked edges — sufficient to compile/test/dump) rather than SPACES-DRAFT-v0.md's real screen prose, pending a future content-only pass (see the status log entry for the full list).
- Rendering writes cells into the `Cell{ch,fg,bg}` grid via `render_cells` only; backends turn cells into pixels/escapes, never the reverse. UI layout: rows `[0, MAP_H)` map, row `MAP_H` status, remaining rows log. Don't draw outside your band.
- **Each cell grades by its OWN light source, not the frame's torch** (batch 14 render fix, playtest-caught). `Game.lit_r: Vec<u8>` (presentation-only, a derived rendering buffer like `vis` — reset each `compute_fov`, filled by `light_circle` with the MAX source radius reaching each tile so the brightest source wins on overlap; NEVER hashed/saved/dumped, so it shifts no `xhash` and needs no `SAVE_VERSION` bump). `render_play` and `scene()` grade every visible cell (tiles, items, blocks, monsters, the player glyph) by `light_pct(lit_r[i])` — so a tile only the McGuffin's shine reaches renders at HER brightness, not the dying torch's murk. The pre-fix code graded the whole frame by one `light_pct(fov_radius(g.light))` (a batch-3 one-light-source leftover), which capped her light at the torch's. **The lesson, not just the fix:** every frame golden stayed byte-identical through this bug because the turn-0 fixtures can never reach a carried-and-shining state (at full torch `light_pct(lit_r)==100` everywhere, so the fix is a provable no-op there) — the guard is the `mcguffin_shine_renders_at_her_brightness_not_the_dying_torch` unit test, which CONSTRUCTS the dim-torch + wide-shine state directly. A from-replay frame golden was investigated and rejected: a *meaningful* one needs her shine radius > the torch radius, which requires burning ~950+ light while carrying (~475 turns) — a late-game state an efficient bot wins or dies before reaching, so it isn't naturally reproducible via a bot replay (the unit test is the right guard).
- Message strings go through `Game::log`. Keep them under ~78 chars so they fit the log row.
- Colors are named `PAL_*` consts in `content.rs` (`PAL_PLAYER`, `PAL_STAIRS`, `PAL_POTION`, `PAL_SWORD`, `PAL_AMULET`, `PAL_LORE`, `PAL_RAT`/`PAL_GOBLIN`/`PAL_OGRE`, `PAL_STATUS`, `PAL_ALERT`, `PAL_LOG_FADE`, `PAL_BAR_HP`/`PAL_BAR_TORCH`/`PAL_BAR_EMPTY`) — one place to retune the palette. `Theme.wall`/`Theme.floor` stay on `Theme` (per-theme, not global).
- Wall autotiling (`WALL_GLYPHS`/`wall_mask` in `render.rs`) is presentation-only: it counts only *seen* neighbors (`in_map && seen && Tile::Wall`) so the glyph reflects what the player has actually observed, not the true map. `--dump`/`level_dump` never call into `render.rs`, so dump goldens stay logical (`#` only) regardless of autotiling changes.
- `Screen` (`Title`/`Play`/`End`) is core render data — `render_cells(g, screen, cells)` takes it as a parameter — but the tiny state machine that decides which `Screen` is active belongs to each backend, not the core. Title-screen dismissal (any key) is consumed without logging an input byte or touching `Game` at all; it's not part of the replay stream.
- `Game::turns` (incremented once per `spend_turn`) is hashed state — it's part of what `state_hash` verifies. `Game::killer` (set in `monsters_act` right before `dead = true`, used by the End screen's death message) is presentation-only and deliberately **unhashed** — don't add it to `state_hash`.
- Light-tier warnings (`spend_turn`'s tier-crossing messages) weave a theme adjective in via `self.adj()` on the `flavor_rng` channel — deterministic, replay-safe, and consistent with the "flavor is grounded" rule above.
- Status bar (row `MAP_H`) draws block-glyph HP/Torch bars (`0x2588` filled / `0x2591` empty) alongside the text fields; the term backend falls back to ASCII (`#`/`-`) for those glyphs since raw terminals can't be trusted for arbitrary Unicode block-drawing without the frame-golden-verified encoder path.
- **MSRV is 1.75, and the toolchain is environment-dependent — CHECK it, never assume** (`rustup toolchain list`). `Cargo.toml` pins `rust-version = "1.75"` (batch 12 R1) as the declared floor: don't use language/std features newer than 1.75, and don't pick crate versions whose MSRV exceeds it. The recurring `E0716` temporary-dropped-while-borrowed slip bit three times under the batch-12 environment — a container running `rustc 1.97.1` with **no** rustup, where `make msrv` fell back to `cargo check` under the ambient toolchain and a clean build was NOT proof (1.97 happily compiles code 1.75 rejects). **But as of batch 16 (2026-07-26) THIS container has rustup with `1.75.0` as the active DEFAULT** (`~/.cargo/bin/cargo` → rustup → 1.75.0), so `cargo`/`make check` here build under GENUINE 1.75 and `make msrv` truly enforces the floor — batch 16 was closed on a real 1.75 `make check` (`msrv: OK (pinned rustc 1.75, both backends)`). Three operational consequences: (a) where a build env lacks real 1.75, reason about 1.75's borrow checker explicitly, a clean build isn't proof; (b) **`make`'s recipes run under `/bin/sh`, which does NOT inherit the profile's `~/.cargo/env` PATH** — so `make` can report `cargo: not found` even when an interactive `cargo` works; prefix `PATH="$HOME/.cargo/bin:$PATH"` when invoking `make` here (this cost the batch-16 close-out a garbled log before it was diagnosed); (c) **binary size shifts with the toolchain** — real 1.75 produces a LARGER binary than the newer ambient `rustc` earlier batches measured under (batch 16: stripped 665,968 / packed 219,776 under 1.75, vs batch 15's 588,992 / 213,008 under the newer ambient toolchain — a toolchain delta, NOT code bloat), so size deltas are only comparable WITHIN one toolchain. Report which toolchain a size was measured under.

## Platform notes

- Linux builds use `minifb` with `default-features = false, features = ["x11"]` to avoid wayland build deps. macOS/Windows builds need minifb's default features — if you add cross-platform CI or docs, that's a target-specific feature flag, not a global change.
- `upx-ucl` and `rustc`/`cargo` install from Ubuntu apt in sandboxed environments (no rustup — its domain is often blocked).

## Roadmap context (so you don't re-litigate v0 cuts)

Deliberately cut from v0, in rough priority order for v0.1+:
1. Audio: small software synth + tracker-style const song data (~20–30 KB budget)
2. Procedural tile sprites replacing/augmenting glyphs (~10 KB budget)
3. Inventory UI + deliberate item use (replaces walk-over pickup)
4. Ranged combat, more monster/item variety

Landed in v0.1 (was cut from v0): save/load — implemented as seed + input log, single slot, no serde.

**Re-sequencing (2026-07-19, human): mechanics before presentation.** The golem-import variety batch (originally scoped as doors/seals, push-blocks + pits, decor mobs, ogre tell, ledger letter-grade — see docs/superpowers/plans/2026-07-19-batch-6-variety.md) jumps ahead of Phase 3; C's presentation stack (sprites/audio/NPC cast) is deferred until the mechanics settle, C-complete guarantee unchanged. The batch is ONE authorized worldgen MAJOR: all channel-touching imports land together, goldens regenerate once. **Landed scope, per the batch's own Amendment 2 rescope once `docs/story/STORY-COMPILE-v1.md` became design canon mid-batch**: portals + authored floors (T1), sokoban push/pit/goal per topdown-puzzle (T2, replacing the seal). DROPPED as not story-priced: the keyed-stair seal, decor mobs, ogre tell, ledger letter grade — see the batch-6 status log entry.

**Endgame direction (2026-07-18, human-approved — see `docs/design/proposals/DECISION.md`):** the adversarial proposal round (A/B/C + SYNTHESIS in `docs/design/proposals/`) is decided. The blended roadmap governs: Phase 1 substrate (save-v2 retry byte + auto-ghost capture, violence tax + sim re-baseline, `scene()` core surface, screen-feel, last-death echo) → Phase 2 mercy system decided BY the pacifist-bot gate → Phase 3 presentation (sprites, NPC-vault cast MAJOR, audio) → Phase 4 ghosts/wasm-mobile/net relay. **Proposal C is adopted in full** (C-complete guarantee: sprite pipeline, full audio incl. leitmotif transposition, wasm mobile with portrait viewport, batch-9 richness pass committed). Sign-offs already granted are enumerated in DECISION.md — don't re-request them; anything NOT listed there still needs fresh sign-off.

**Direction (2026-07-18, per the human): rl144 is drifting engine-ward, not just game-ward.** Two win conditions: (a) a ≤1.44MB executable game, (b) a tiny MMORPG. Networking is therefore no longer permanently cut — it may return behind a compile-time feature flag, built as lockstep input-sharing over the existing deterministic replay core (state = `replay(seed, input_log)`; multiplayer = relaying input bytes, never serializing the world). This makes channel discipline and replay convergence engine API, not test hygiene. Still cut permanently: mod support, config files, localization.

**`make check` is the whole gate in one command** (`make UPX=/path/to/upx check`): build, test, test-term, golden cmp (temp-dir, tree untouched), frame-golden cmp, cross-backend `xhash`, `--solve 10000`, `--sim 5000` (all four policies — greedy, pacifist, tactical, tactical-pacifist — each against its own band file), UPX size budget — see Verification above for what each step covers. Run it before calling anything done.

## Definition of done for any change

1. `cargo build --release` clean (warnings count as not clean).
2. `cargo test` green.
3. `--dump` (and any relevant headless mode) output eyeballed and sane; `--solve 10000` green after worldgen-adjacent changes.
4. Both size numbers (stripped, packed) reported, with delta vs. baseline.
5. Anything unverifiable headlessly explicitly flagged for human playtest.
6. If you changed gameplay behavior, update this file's conventions section when the change makes a stated invariant false.

## Subagent dispatch discipline

This project is built almost entirely through dispatched subagents (one implementer per task → a reviewer → fix round(s) as needed → next task; a final whole-branch reviewer before every merge). The discipline is about model tier and parallelism, not about avoiding blocking calls:

- **Model tiers**: Fable or Opus for planning and final review (writing/adjudicating batch briefs, the whole-branch review before a merge — judgment-heavy, low-volume work). Sonnet for implementer/worker subagents — the actual per-task code-writing, especially anything parallelizable (independent tasks with no shared state, e.g. two content-drafting tasks, or a code task alongside a docs task). Dispatch parallel Sonnet workers whenever tasks don't depend on each other; don't serialize work that doesn't need it.
- **Running out of session/usage limit mid-batch is expected and fine, not a failure mode to engineer around.** It is recoverable: use `ScheduleWakeup` (or the `/loop` skill in self-paced/dynamic mode) to schedule a check-in — every 20–30 minutes is a reasonable default cadence — so work resumes automatically once the limit clears, picking back up from `git status`/`git log` and the local `.superpowers/sdd/progress.md` ledger. Don't contort dispatch prompts trying to prevent hitting the limit; just make sure state (branch, commits, ledger) is always in a resumable place when you stop.
- **State the acceptance numbers up front when they're knowable** (a pure refactor's expected byte-identical gate values; an unchanged sim/solve baseline a task must not perturb) so the implementer can self-verify against a concrete target instead of an implicit "should still work." Independently re-run the empirical claims in every review rather than trusting a report's numbers — this repo's review passes have caught real bugs this way (a bot stuck in an infinite loop, a killing-blow visual effect that silently never rendered, a save-format claim that was actually correct but needed tracing to confirm).

One session-management habit worth keeping: when a batch's story/design work implies a *new* mechanism (not just new content), write it into the relevant staging doc with an explicit "needs its own sign-off" flag rather than quietly building it — see how `SPACES-DRAFT-v0.md`'s screen-link glyph is marked above. Undersized asks get missed; flagged ones get their own brief.

**Read the canon before pronouncing on it.** A scope call — "that's post-1.0", "that's cut", "that's one batch" — is only valid when it cites the human-locked doc (`docs/story/STORY-COMPILE-v1.md`, the arc doc, `DECISION.md`), never memory of it. The road-to-1.0 plan mis-scoped §9-F twice — the planner put the coat/mimic/lost-guy cast on the cut list and an implementer turned it into a self-use dodge — and both were pronouncements from recollection; only a third pass that actually re-opened §9-F found that the cast IS the descent and the minigames ARE the ending (the mantel exam reuses the coat's and mimic's lessons), a structural fact no one's memory held. Before scoping any §N ask, open §N. It's cheap, and unlike a lesson held in one session's memory it survives the session death this whole section is about. (Recorded as the plan of record in `docs/design/2026-07-26-road-to-1.0.md`, which reads *from* the canon and defers to it — "the ratchet, not the authority.")

**Canon precedence — conflicts auto-resolve UPWARD, silently, no ruling needed** (human directive, 2026-07-27). The authority chain is `docs/story/STORY-COMPILE-v1.md` > the arc/design docs (`docs/design/*.md`, e.g. the mercy-economy arc doc) > plans/briefs (`docs/superpowers/plans/*`). When two disagree, the HIGHER one wins automatically — a lower doc that contradicts a higher one is a BUG in the lower doc, not a decision to adjudicate: fix the lower doc to match (or record an explicit human override at the point of conflict). Established when the cast brief (a plan) RULED a stat-flag disarm that contradicted STORY-COMPILE §5/§9's sword→`Hold` + set-down-for-regard model — canon won, the plan was corrected. The one exception is an explicit human override for a specific point, which is recorded inline where it applies.

**Fold remaining questions into the ONE sign-off package as recommendations-with-defaults — do NOT serialize pre-rulings** (human directive, 2026-07-27). A MAJOR/brief that surfaces open design questions presents them INSIDE its single sign-off package, each as a recommendation plus the default taken absent objection — not as a back-and-forth of one-question-one-answer rounds before the package exists. One package, one sign-off, then build. The canon-VERIFICATION pass still precedes the package (read-canon-before-pronouncing is unchanged), but its findings land as folded recommendations, not serialized blocking questions.

## Status log (append-only; date each entry)

- **2026-07-18 — v0.1 landed.** Channel RNG (`h64`/`channel`, worldgen frozen by goldens); `--solve` winnability + difficulty-band gate (10K seeds, band in `tests/solver-band.json`); torch mechanic (run-wide light pool, tiered FOV, `START_LIGHT=2000` derived from worst-case round-trip budget 1503); return-trip win with up-stairs and persistent `LevelState` snapshots; exit/amulet in BFS-deepest room; 4 const themes with grounded slot-filled lore; 3 ASCII vaults stamped via their own channel; save/replay as seed + input log (`--replay`, `--load`, F5). Sizes (local rustc 1.97, Arch): stripped 478,680 B, packed 173,176 B (baseline was 440,912 / 157,664 on this box; Ubuntu 1.75 baseline in this doc predates it). Two authorized worldgen MAJOR re-baselines: task 3 (deepest-room placement + `<`), task 5 (vaults).
- **2026-07-18 — human playtest:** F5 save + `--load` resume confirmed working in the window. Still unverified interactively: light-tier warning pacing, new status bar readability, stair-transition feel, whether the ~33% light margin plays fair.
- **2026-07-18 — golem cheap wins + engine reframing.** `hash_vectors` test freezes the h64/channel primitive directly; `--solve --report` (ungated stats); `--daily` shared seed; room kinds+tones (message-only, goldens verified byte-identical); story-buried-by-depth lore inscriptions (`?` items at BFS shallow/mid/deep rooms — authorized worldgen MAJOR, goldens regenerated, solver stats unchanged); resizable window over the fixed 80×30 grid (needs playtest). Roadmap updated: engine direction, networking may return behind a compile flag. Also: world identity in-game — seed in the window title, F1 logs `seed + world_hash` (FNV over the 5-depth dump: names the generator's *output*, so it shifts exactly when a worldgen MAJOR would).
- **2026-07-18 — batch 2 (subagent-built) + module split.** `--sim N` deterministic greedy bot (drives the engine purely through input bytes; **finding: 0% win rate over 5000 seeds, 100% combat deaths, 0 darkness deaths — combat lethality, not the light budget, is the wall; balance pass needed, requires sign-off**); theme-tinted rendering + low-light brightness tiers (playtest pending); string seeds (`--seed swordfish`); saves renamed to `rl144-<worldhash>.sav` with F5 double-press overwrite confirm and autosave-on-quit (never clobbers a manual save; playtest pending); `Makefile` `check` gate; `src/main.rs` split into rng/content/game/headless/save/render modules (pure motion, gate-verified, output byte-identical). Packed size 189,072 B (12.8% of budget).
- **2026-07-18 — batch 3: combat balance sign-off, core/crust seam, terminal backend, aesthetic pass.** Five tasks, all `make check` green.
  - **Combat balance (authorized worldgen MAJOR, signed off)**: fixes the batch-2 0%-win wall. `+4` maxHP and a heal on each depth's first descent; spawn count `3 + d`; roll `d10 + d`, thresholds `rat < 9`/`goblin < 13`/`ogre >= 13`; item drops `+2*(d-1)`. `--sim 5000` = 14.6% wins (729/5000), 5 dark deaths, 0 stuck, 4266 combat deaths — inside the new `tests/sim-band.json` band (`win_pct [10,25]`, `deaths_dark [1,500]` as a raw count, not a percent share). Goldens regenerated (spawn glyph placement/counts only; wall/floor/stairs/theme/lore layout verified byte-identical by diffing with monster/item glyphs normalized out). Solver band re-verified unchanged: `min 636 / p50 954 / p90 1110 / p99 1272 / max 1503`, worst seed 6592 — confirms worldgen truly untouched. (Also found and fixed in passing: a stale `tests/solver-band.json` comment whose prose numbers predated two earlier authorized MAJORs; the numeric bands themselves were never wrong.)
    **Honesty note**: dark deaths are structurally rare for the BFS-optimal `--sim` bot — it walks efficiently enough that combat, not the light clock, is almost always what kills it. Pushing dark deaths meaningfully higher would need a `START_LIGHT` or amulet-burn change, which is outside this batch's balance sign-off; flagged for a human decision before touching either constant.
  - **Core/crust seam**: `render.rs` reduced to a platform-free cell composer (`render_cells(&Game, Screen, &mut [Cell])`, `Cell{ch:u16,fg:u32,bg:u32}`); the minifb window loop moved verbatim into new `src/backend_minifb.rs`; a new ANSI terminal backend landed in `src/backend_term.rs` (hand-rolled termios raw mode via direct libc FFI — no new crate — dirty-cell ANSI/256-color encoder, panic-safe terminal restore via a chained panic hook). Two cargo features (`backend-minifb` default, `backend-term`) gated by `compile_error!` for the zero/both cases. `--render-frame --seed N [--ascii]` added as the term backend's headless verification surface, frozen by new frame goldens.
  - **Cross-backend `xhash` gate** (`make xhash`): replays `tests/fixtures/ref.sav` through both backend binaries and requires an identical state hash — the concrete proof the core/crust seam holds, since it compares the backends to each other rather than to a frozen constant.
  - **Aesthetic pass**: wall autotiling (`WALL_GLYPHS`/`wall_mask`, seen-neighbors-only, presentation-only — dump goldens verified byte-identical); Title/Play/End screens (core render data, backend-owned state machine); deepened light-tier grading (100/85/70/55/40/28, was 100/90/80/65/50/40) with theme-adjective warning text; palette pulled into named `PAL_*` consts in `content.rs`; block-glyph HP/Torch status bars (term backend falls back to ASCII). font8x8's `unicode` cargo feature dropped — only the always-compiled `legacy` bitmap tables are used, direct-indexed, bit-identical glyphs, smaller binary.
  - **Size scoreboard** (`make targets`, this batch's final numbers):

    ```
    target       stripped       packed    pct
    minifb         523960       192172    13%
    term           398504       169724    11%
    ```

    Both flavors well under the 1,474,560-byte per-target budget.
  - **Consolidated playtest-pending list** (nothing here is verified interactively; everything is compile-checked/headless-verified only): batch-2 carryovers — light-tier warning pacing, stair-transition feel, whether the ~33% light margin plays fair, theme-tinted rendering readability, F5 overwrite-confirm flow, resizable window behavior; new this batch — title/end screen layout and pacing (both backends), terminal backend feel (raw-mode entry/exit, ESC/arrow-key timing, alt-screen visuals, Ctrl-C handling), low-light grading oppressiveness at the deepened tiers, status-bar readability (bar/text contrast, term ASCII fallback legibility) in both backends, `--load` skipping the Title screen entirely. Four known-small minors carried without action: the term backend's escape-sequence reader doesn't drain a trailing `~` on an *unrecognized* CSI sequence, its raw read/write helpers don't retry on `EINTR`, `draw_status` takes 9 positional same-typed params, and the wall-mask tests carry ×6 boilerplate.
- **2026-07-18 — batch 4 / Phase 1: violence tax, save v2 + ghosts, scene() surface.** Per `docs/design/proposals/DECISION.md`'s blended roadmap (Phase 1 substrate, sign-offs pre-granted — see that file). Three landed tasks plus this docs task, all `make check` green.
  - **Violence tax (authorized worldgen-adjacent change, signed off, DECISION.md item 1)**: `VIOLENCE_TAX = 1` (`game.rs`) — every bump-attack now burns 1 extra light on top of the per-turn cost, folded into `spend_turn`'s single deduction (light-0 check still runs exactly once, lose-before-win holds; a killing blow whose tax lands light ≤ 0 is correctly a darkness death). Wall bumps stay free. `--sim 5000`: wins 729→726 (14.6%→14.5%), `deaths_combat` 4266 (**unchanged** — the greedy bot only fights when routing forces it through a monster, so the tax rarely flips a run's outcome; humans, who fight more speculatively, will likely feel this tax more than the bot does), `deaths_dark` 5→8, `stuck` 0. `tests/sim-band.json` re-baselined: `win_pct [10,25]` unchanged, `deaths_dark` tightened to `[2,40]`. No spawn/roll-table knobs touched — worldgen goldens and `tests/solver-band.json` verified byte-identical/unchanged.
  - **Save v2 + RLG1 ghosts (DECISION.md item 2)**: input byte 6 (`INPUT_RETRY`) reconstructs `Game::new(seed)` on the *same* seed (vs. byte 5's reroll), carrying a new presentation-only `Game.echo` field (death position/depth) forward from the just-ended attempt. `SAVE_VERSION` bumps to 2; `parse_save` still accepts v1 blobs and replays them byte-identically (proven by a dedicated test — a v1 log can never contain byte 6). New `RLG1` ghost format: pure builders/parsers in `save.rs` (zero I/O), backends own the actual file write, labels drawn only from the const `GHOST_LABELS` table (no free text, ever). Both backends auto-capture a ghost (`rl144-<worldhash>.ghost`, latest death wins) on R/N/Q at the End screen after a death, never a win. End-screen legend now reads `[R] retry this world  [N] new world  [Q] quit`; N now does what R used to (reroll). Phase 1 substrate only — no ghost rendering/playback caller yet, so `Ghost`/`parse_ghost` carry `#[allow(dead_code)]`. `xhash c53b82a314027d11`. Carried minor: `GHOST_LABELS`' length isn't asserted divisible by 4 (the outcome-band-sizing formula silently assumes it).
  - **`scene()` core surface + minifb screen-feel (DECISION.md item 5)**: `scene(&Game) -> Vec<SceneEntity>` in `render.rs` — additive to `render_cells`, derived-only (facing/anim_phase/light_pct per visible entity; `SpriteKind` incl. `Echo`), zero new hashed/saved state beyond `Game.facing` itself (also presentation-only). New `Game.fx_hit` (last melee impact tile, presentation-only) drives a minifb-only flash + vertical-squash effect on the struck cell; the term backend gets a doc note explaining why it skips screen-feel this phase (dirty-cell diffing, no sub-cell resolution). The last-death echo renders as a dim `@` in `render_play`, gated on all three of: `g.echo` is `Some`, its depth matches the current depth, and the tile has been `seen` — same never-leak-unseen-topology rule `wall_mask` already follows. **Review round caught a Critical**: the killing-blow flash was computed but the Play→End screen transition happened in the same loop iteration, before the bottom-of-loop render — so the flash frame was never actually shown. Fixed with a one-frame-deferred transition (`pending_end`, `backend_minifb.rs`): the flash renders for one frame before End takes over, deterministic and input-log-neutral (no new `apply_input` call, nothing new logged). `--sim 5000` identical to the save-v2 task's numbers (726 wins/8 dark) — proves zero gameplay leakage from a purely presentational task.
  - **Sizes** (`make targets`, this batch's final numbers):

    ```
    target       stripped       packed    pct
    minifb         527000       193332    13%
    term           400264       170288    11%
    ```

    Both flavors well under the 1,474,560-byte per-target budget; minifb grew ~4KB stripped / ~1.2KB packed over batch 3 across all three tasks combined.
  - **Playtest-pending additions this batch** (alongside the standing batch-2/batch-3 list above): End-screen R/N/Q flow and the silent ghost-capture-on-death (does the player notice/care that a ghost was written?), flash/squash hit-feel readability and timing, echo distinctiveness from a live monster/player glyph at a glance.
  - **Next**: Phase 2 (mercy system) is decided BY the pacifist-bot gate per DECISION.md's blended roadmap — not a drive-by, needs its own dispatch.
- **2026-07-19 — batch 5 / Phase 2: mercy as talk (the Henson ruling).** Per DECISION.md item 3 and `docs/superpowers/plans/2026-07-19-batch-5-mercy.md` (+ addendum). The arc in three sentences: mercy-as-talk landed (talk bytes 7–10, receptivity-rolled becalm, save v3); the first measurement showed pacifism strictly DOMINANT over greedy combat (22.8% vs greedy's 14.5% wins — the original flat, guaranteed stayed swing denied every talked-to monster its retaliation swing for free, no cost, no risk); the human-directed parley receptivity roll ("needs to be algorithm'd") repriced it to 7.9% — right-but-harder, and the sim bot's persist-through-failure policy (a failed talk neither stays nor moves its target, so the bot just re-talks the same blocker next turn rather than giving up) makes 7.9% a floor for unskilled play, not a ceiling: fight-then-spare a wounded ogre is now the strong line for a human player, since `receptivity()`'s `40*(maxhp-hp)/maxhp` term means wounds open ears.
  - **T1 — mercy core** (`992b1df`+`750124a`+`2af3c85`): talk bytes 7–10 (byte order mirrors move bytes N/S/W/E); `Monster.regard`/`Monster.calm` and `Game.spared` all hashed (mercy is run-defining, not presentational — the `killer`/`echo`/`facing`/`fx_hit` exclusion set is unchanged); becalm at `talk_threshold` (rat 2/goblin 3/ogre 4); stayed swing as a transient per-turn parameter threaded through `monsters_act` (never stored, never hashed); `TALK_LINES` grounded talk-line table; `SAVE_VERSION` 3, `1..=3` accepted. Greedy sim reproduced exactly (726/4266/8/0) — zero leakage from a mechanic the greedy bot never touches. Sizes 530,472/194,344. Review: two grounding-fix rounds — the reviewer read all 72 talk-line fillings and rejected four strings for inventing unproven history or props, including one falsifiable "no prior attack" claim; both rounds reworded the strings in place, register preserved.
  - **T2 — pacifist bot + gate** (`7ac38d1`): `--sim N --policy pacifist` — talk instead of bump-attack on any non-calm blocker, otherwise identical loot/route logic. First cold measurement, pre-parley mechanic: 1140/5000 = 22.8% wins, 0 kills, 73,345 spared, 19 dark deaths — band `[15,30]`. **Finding, measured not assumed: pacifism was strictly dominant.** The guaranteed stayed swing meant every talked-to monster lost its retaliation turn for free, while a bump-attack survivor hits back immediately — mercy was the *easier* route, not the harder one the violence tax was supposed to buy. Greedy control re-run unchanged (726/4266/8/0), proving no shared-knob leakage.
  - **T3 — frontends** (`f058051`+`65279d1`): `t` (or `a`) + direction chord → one logged talk byte, identical semantics both backends; becalmed tint (`PAL_CALM_TINT`). Review fix round caught two real bugs: a held-repeat key (Period/Wait) failed to disarm an armed chord in minifb, and term's `t,t,dir` sequence didn't parity-match minifb's re-arm rule, logging the wrong byte for the same keystrokes; both fixed, plus an adopted cancel-feedback improvement on term's dead-chord path. Playtest: chord feel both backends, becalmed tint readability.
  - **Rename + parley repricing** (`915e343` docs, `7f7c64c` rename, `d85bb0c` parley): human ruling renamed ACT to talk throughout (`try_act_player`→`try_talk_player`, `act_threshold`→`talk_threshold`, chord/backend identifiers, every comment and test) — "ACT" was Undertale menu jargon naming a UI action, not what the mechanic does; zero behavior change, verified by exact sim reproduction and unchanged xhash. `receptivity()` (`game.rs`) then replaced the flat counter: `BASE[kind]` (rat 55/goblin 35/ogre 20) `+ 18*regard + 40*(maxhp-hp)/maxhp + 6*(atk-3) - 10` if torch FOV radius ≤ 4, clamped `[5,95]`, rolled against a new `parley` channel; a landed roll advances regard and stays the monster, a failed roll does neither, so it can retaliate. Pacifist re-measured at 5000 with the addendum's starting constants left untouched — no tuning needed on the first try: 394/5000 = 7.9% wins (`deaths_combat` 4599, `deaths_dark` 7, `stuck` 0), inside re-baselined `tests/pacifist-band.json` `[5,20]`. Greedy stayed bit-exact throughout. Review: APPROVE, zero findings, all numbers reproduced live.
  - **Numbers**: greedy sim 726/4266/8/0, `win_pct` in `[10,25]` (`tests/sim-band.json`, unmoved all batch); pacifist sim 394/4599/7/0, `win_pct` in `[5,20]` (`tests/pacifist-band.json`); xhash `c9c33d5824ca4501` → `c24171626b476b07` (expected — `parley` joins the hashed RNG-state list, both backends agree on the new value); sizes 533,064 stripped / 195,392 packed (13.25% of the 1,474,560-byte budget).
  - **Thin-margin flag**: pacifist's measured 7.9% sits close to the band floor of 5 — the addendum's starting constants cleared the floor on the first measurement, not under tuning pressure, so a future `receptivity()` constant tweak (or a `talk_threshold` change) can trip the `[5,20]` STOP clause by design. That's the gate working as intended, but the margin is real and worth remembering before the next mercy-adjacent change.
  - **Playtest-pending additions** (alongside the standing lists from batches 2–4): talk chord feel in both backends (arm/disarm timing, the two-keystroke rhythm), becalmed tint distinctiveness, unmoved/failed-talk line pacing, and whether the 55/35/20 receptivity bases actually feel like rat/goblin/ogre temperaments in play.
  - **Rename rationale, one line**: ACT → talk because "ACT" was borrowed Undertale menu jargon naming a UI action, not the mechanic itself — the verb the engine, both backends, and this doc now use, top to bottom, is talk.
- **2026-07-19 — batch 6 / variety MAJOR: portals + authored floors, sokoban, story canon adopted.** Per `docs/superpowers/plans/2026-07-19-batch-6-variety.md` (+ two amendments). Human re-sequencing: mechanics before presentation (sprites/audio/NPC cast stay deferred). ONE authorized worldgen MAJOR — every channel-touching feature landed together, goldens regenerated once at the end.
  - **Story canon adoption + rescope (Amendment 2, `035e68f`+`7e0a1c9`).** `docs/story/STORY-COMPILE-v1.md` + `FLAVOR-DRAFT-v0.md` landed as human-locked design canon mid-batch. Reconciliation DROPPED from this batch (not story-priced, per the canon's own Rule 4 "new mechanics are priced, never assumed"): the keyed-stair seal, decor mobs, ogre tell, and the ledger letter grade — all four were in the original brief and none survived contact with the story doc. T2 was rescoped from "pits + blocks + seal" to sokoban proper, studied from `~/Documents/gitrepos/golem/games/topdown-puzzle/shared/push.js` and priced against story §6.3/§9-H. Batch 7+ now implements the story's §9 asks in its own order (A first); the T1 `AUTHORED_FLOORS` machinery is the enabling tech for §9-J's fixed overworld screens. Two stale WIP stashes covering the dropped seal work were carried through the batch and dropped at closeout (see below).
  - **T1 — portals + multi-world state + authored floors** (`d27be7c`, perf fix `2c47298`): see this file's Code-conventions entries on portals/authored floors for the mechanism. Goldens diff was purely additive (`.` → `*`, nothing else moved) and the solver was byte-identical, confirming portal placement alone doesn't touch layout. `xhash c9a97603dc335f22`; sims 733 wins/8 dark (greedy), 396/7 (pacifist), both in-band; sizes 198,908 packed (13.5%). Review: Approved, zero blocking findings; two minors fixed in the perf commit (portal-describe world-hash was recomputed fresh on every walk-onto — regenerating all 5 depths of the destination world per step — now memoized at generation time in `Dest::World`'s second field) and left carried (an unreachable regen fallback stays silent by design).
  - **T2 — sokoban** (`4ed9659`): push chains capped at `MAX_PUSH_CHAIN = 2`, farthest-member-first resolution, pit-fill, goal-lock, 2 solution-tested vaults (`VAULTS` indices 3–4: "the bridge", "the goal cell") added to the existing 3. Review: APPROVE, zero findings. **Sim caught two real bugs before review reached them**: (1) an early `bfs_dist` that treated blocks as blocking severed a sokoban room's chain-graph connectivity from itself, mis-scoring reachability inside the puzzle; fixed by making the routing BFS blocks-*oblivious* (blocks are pushable obstacles, not permanent walls — only `Tile::Pit`/`Tile::Wall` block routing) while keeping `Game::gen_level`'s exit-room selection excluding sokoban vault centers, so a block landing on the level's own down-stairs can never wedge a `--sim` bot permanently. Both fixes are load-bearing, not cosmetic — see `bfs_dist`'s and `Game::gen_level`'s doc comments for the mechanism. Sims drifted in-band from the vault-channel pool growing (same draw index can now land a different, differently-shaped vault): greedy 953/15 dark, pacifist 631/16 dark — no band-file re-baseline needed, both stayed inside the standing `[10,25]`/`[5,20]` win-pct ranges and `[2,40]` dark-death range. `--solve 10000`: 0 unwinnable, still in-band. `xhash d92834c3cd14adb2`; sizes 547,016/200,548 (13.6%).
  - **T3 — goldens regen + full gate + this doc** (docs+goldens only, no src changes). Regenerated all 5 dump goldens (seeds 1/2/3/42/1337) and both frame-golden fixtures (`--render-frame` seeds 1/42 + seed-1 `--ascii`, term backend) once, after T1+T2 both landed. Dump diff eyeballed on seed 1: changes are exactly `*` portal additions plus vault-stamped-room content shifts (new `B`/`^`/`x` glyphs, monster/item count deltas, and — where a sokoban vault's differently-shaped footprint displaced the room a prior vault or the exit had occupied — a stairs/`>` relocation within the same depth); wall/floor/corridor structure everywhere OUTSIDE a vault-stamped room footprint is byte-identical to the pre-batch goldens, confirming the MAJOR's structural surface is exactly "additive portals + vault-pool-driven room content," nothing else moved. Frame goldens for seeds 1/42 came back byte-identical to their pre-batch fixtures — both seeds' depth-1 initial frame happened not to roll a portal or vault-content change, so there was nothing for the frame capture to see; not a bug, just where the dice landed. `make check` (`build`, `test` ×2 backends [74+78 tests], `goldens`, `frames`, `xhash`, `solve`, `sim` ×2 policies, `size`) green end-to-end with zero band-file edits: `xhash d92834c3cd14adb2` (unchanged from T2, as expected — a docs+goldens-only task can't perturb replay); `--solve 10000` min 615/p50 951/p90 1107/p99 1263/max 1494, worst seed 2108, 0 unwinnable (all inside `tests/solver-band.json`'s standing ranges); `--sim 5000` greedy 953 wins (19.1%)/4032 combat deaths/15 dark/0 stuck (inside `tests/sim-band.json`); `--sim 5000 --policy pacifist` 631 wins (12.6%)/4353 combat deaths/16 dark/0 stuck (inside `tests/pacifist-band.json`). Sizes: stripped 547,016 B, packed 200,548 B (13.6% of the 1,474,560-byte budget) — unchanged from T2 since this task touched no source.
  - **Stash cleanup**: two stale WIP stashes predating the Amendment-2 redirect (`stash@{0}` "batch-6 T2: pre-existing stale WIP... keyed-seal + non-chain push, superseded by Amendment 2" and `stash@{1}` "stale pre-redirect doors/keyed-stair WIP, broken, out of scope for T1 portals") both covered the dropped keyed-stair seal and were never applied to any landed commit; dropped at this closeout, `git stash list` now empty.
  - **Playtest-pending additions** (alongside the standing lists from batches 2–5): portal transit feel (walking onto one, waiting to transit, the "you step through" arrival line pacing), portal-describe line pacing/legibility next to ordinary log lines, the two starter authored floors (do they read as distinct "places" rather than more dungeon?), sokoban push feel (weight, the chain-of-2 cap, pit-fill/goal-lock feedback), and new-glyph readability at a glance (`*` portal, `^` pit, `x` goal, `B` block against the existing palette, in both backends).
- **2026-07-19/2026-07-20 — batch 7 / cartridge split: the unbraiding + give/use verbs + cartridge-purity fixes.** Per `docs/superpowers/plans/2026-07-19-batch-7-cartridge.md`. Human direction: "none of the specific game stuff is integral engine stuff... each other game is a YAML file or equivalent" — interpreted as `GameDef`, a pure-data contract (see the new Cartridge doctrine section above). Three tasks, all `make check` green.
  - **T1 — the unbraiding** (`c83606b`, opus review Approved). Pure refactor, zero behavior change: every game-specific fact moved out of `game.rs`/`content.rs` into `src/gamedef.rs` (the `GameDef` type contract) and `src/games/contractor.rs` (`pub(crate) const GAME: GameDef`, cartridge #1). Reviewer independently re-verified byte-identity across all five gates plus a ~25-value manual transcription audit (spot-checking authored constants against their pre-refactor source) — clean. Proof numbers: dump/frame goldens byte-identical, `xhash d92834c3cd14adb2` unchanged, `--solve 10000` identical stats (min615/p50 951/p90 1107/p99 1263/max 1494/worstSeed 2108), `--sim 5000` identical both policies (greedy 953/15 dark, pacifist 631/16 dark), `cargo test` 74/78 green both feature sets. Sizes 551,352 stripped / 201,340 packed (+792 B packed vs. the 200,548 baseline — well inside the brief's +6KB indirection allowance). `docs/story/SPACES-DRAFT-v0.md` (staged overworld/vault content, not yet built) cherry-picked in alongside as `f92b788`.
  - **T2 — give/use verbs + cheese/coat/towel, story §9-A** (`7e329fc`+`a249dd2`, opus review Approved after one fix round). Input bytes 11–14 = give-N/S/W/E (directional, mirrors talk/move byte order), 15 = use (self-apply, no direction); vocabulary now 0–15, `SAVE_VERSION` 3→4 (`1..=4` accepted, old logs replay unaffected — proven by test). `Game.held: Vec<u8>` (LIFO, hashed) backs both verbs; `ItemDef::on_pickup` can now be `Hold` (push onto `held`) instead of the original `Consume` (instant walk-over apply) — the **potion** moved from `Consume` to `Hold`+USE this batch, the first item to do so. `GameDef::give_table` rows landed: cheese→rat (regard PENALTY, `[TUNE -2]`, story §4's "everyone knows rats want cheese... measured regard PENALTY"), potion→any monster (regard `[TUNE +3]`, heals the target to full — the single biggest regard event in the game, NAR_035 verbatim). Cheese also burns for a light flicker on USE (`[TUNE +8]`). Coat and towel ship as items + grounded pickup lines only (`[`, `~` glyphs) — no give-target yet, since neither's target monster (the coat-monster, the lost guy) is built; `give_table` gracefully no-ops via `StringsDef::give_declined` for any (item, monster) pair with no row, which is the mechanism that makes this partial state non-broken. Bots never emit 11–15 (asserted); bands untouched, but sims re-baselined against the new item-timing (loot slots can now draw cheese, and the bot's HP-triggered potion-USE changes routing slightly): greedy 953/19.1%/15 dark → **849/17.0%/14 dark**, pacifist 631/12.6%/16 dark → **464/9.3%/11 dark** (win_rate 0.093 = 464/5000), both still comfortably inside the standing `[10,25]`/`[5,20]` win-pct bands and the `[2,40]` dark-death band — no band-file edits. `xhash d92834c3cd14adb2` → **`6aa72d29dc62f5bb`** (expected: `held` joins hashed state, and the vocabulary/save-version bump changes what a replay can contain). Goldens regenerated; reviewer programmatically verified the diff is item-content-only (new cheese/coat/towel spawns changing which loot slot draws what, no structural/layout change) — not a fresh worldgen MAJOR sign-off beyond what item-table changes already require under the existing doctrine. **Real bug caught and fixed mid-task**: the greedy sim bot's potion-USE trigger originally checked `held.contains(POTION)`, which could fire USE while a potion sat buried under something else in `held` (GIVE/USE only ever reach `held.last()`) — a bot could think it had a usable potion and be wrong, silently wasting the turn. Fixed to `held.last() == Some(&POTION)` before the sim numbers above were taken as baseline. Grep-clean self-corrected mid-task (a first draft leaked an item noun into engine code; caught and fixed before review). Fixed in the same arc: the title-screen legend was missing the new give/use keys (a real player-facing gap, not cosmetic — `a249dd2` adds "Give: g+dir    Use: u") and a stale `tests/sim-band.json` comment referenced a renamed `BALANCE` field. Sizes 555,624/202,824.
  - **T3 — cartridge-purity fixes + this doc** (docs + two small fixes, no gameplay/worldgen changes). Two carried minors from T1's review, both in files T3 owns per the brief: (1) `render.rs`'s status-bar draw hardcoded the literal `"Torch ["` — a cartridge-doctrine violation (the engine shouldn't know the light resource is called "Torch"; a future cartridge might call it "Battery" or "O2"). Added `StringsDef::resource_label` (`gamedef.rs`), set to `"Torch"` in `contractor.rs`, `render.rs` now reads `GAME.strings.resource_label` — confirmed via grep that no `"Torch` string literal remains in `render.rs` (surviving mentions are prose in doc comments, not data that ships). (2) `save.rs`'s `parse_ghost` read `label_idx: u8` from untrusted ghost-file bytes with no bounds check against `GAME.ghost_labels.len()` (cartridge-variable, currently 12); an out-of-range value would have survived into the `Ghost` struct and become a live out-of-bounds index once ghost playback (still dead code, Phase 4) lands. Added the bounds check (`>= GAME.ghost_labels.len()` → parse fails, returns `None`) plus a new test (`parse_ghost_rejects_out_of_bounds_label_idx` in `main.rs`) proving `label_idx == len()-1` parses and `label_idx == len()` fails. Neither fix touches gameplay-visible behavior: `xhash` unchanged at `6aa72d29dc62f5bb`, `--solve 10000` and both `--sim 5000` policies reproduced exactly (min615/p50 951/p90 1107/p99 1263/max 1494/worstSeed 2108, 0 unwinnable; greedy 849/17.0%/14 dark; pacifist 464/9.3%/11 dark), goldens/frames byte-identical (render-only and dead-code-path-only changes, as expected). `cargo test`: 89 passed (default) / 93 passed (backend-term), up from 88/92 pre-fix (+1 new test, both feature sets). Sizes: **555,768 stripped / 202,704 packed** (13.7% of the 1,474,560-byte budget) — vs. T2's 555,624/202,824, i.e. +144 B stripped (the new field + bounds check + test) but −120 B packed (UPX compression variance on the changed code layout); vs. the pre-batch-7 baseline (547,016/200,548), the whole batch's net cost is +8,752 stripped / **+2,156 packed**, comfortably inside the brief's ≤+10KB packed budget for T1+T2+T3 combined.
  - **Playtest-pending additions** (alongside the standing lists from batches 2–6): give/use chord feel in both backends (`g`+direction, bare `u`, alongside the existing move/talk chords), title-screen legend readability now that it carries a fourth line of key hints, and item-glyph readability at a glance for the three new/changed items — cheese `o`, coat `[`, towel `~` (potion `!`, unchanged glyph, now a held item rather than instant-consume, which changes what walking over it *feels* like even though the glyph didn't move).
- **2026-07-21 — batch 8 / voice two comes online: the McGuffin's voice (story §9-B/C/D).** Per `docs/superpowers/plans/2026-07-19-batch-8-mcguffin.md`. Human-locked design canon (`STORY-COMPILE-v1.md`/`FLAVOR-DRAFT-v0.md`) gets its second mechanical voice: the McGuffin now comments on being carried, dropped, climbed with, and picked back up. Three tasks, all gate-verified green.
  - **T1 — engine hooks: `CarryEvent` dispatch, byte 16 put-down, `speech_attempts`** (`f7d8ec1`). New generic engine surface: `GameDef::carried_lines: &[(CarryEvent, &[&str])]` + `GameDef::carried_preamble: &[&str]`; `Game::carry_event(ev)` fires only while the objective is carried, and is a provable no-op (no RNG draw, no state mutation) when `carried_lines` has no row for `ev` or that row's pool is empty — the invariant load-bearing for this task's byte-identity claim, since T1 shipped with an empty `CARRIED_LINES`/`CARRIED_PREAMBLE` in the contractor cartridge. `CarryEvent` variants: `PickedUpBloody`/`PickedUpMerciful` (split from a single flat `PickedUp` mid-task — see the finding below), `PutDown`, `PickedBackUp`, `StairsUp`, `MonsterAdjacent`, `KillWitnessed`, `SpareWitnessed`, `TierCrossed`, `Idle` (unit, not `Idle(n)` — see the deviation note below). Byte 16 (`Game::put_down`, story §9-D) is self-apply like byte 15, no direction: sets the objective back into the current tile's item list, flips a new hashed `objective_dropped` bool, fires `CarryEvent::PutDown`, and the carry-burn light multiplier reverts to base automatically through the existing `spend_turn` path (carry-burn was already conditional on `has_objective`, so no separate revert logic was needed). `speech_attempts: u8` (hashed) is the climb re-entry ladder: `CarryEvent::StairsUp` fires and indexes its line pool by this counter BEFORE it increments, so the first carried ascent reads rung 0; a future rung beyond the pool's length clamps to the last entry (steady-state line). `Game.mcguffin_last_line_turn` (new, presentation-only) is the rate-limit clock and joins the `killer`/`echo`/`facing`/`fx_hit` exclusion set (now five fields, `save.rs`'s `state_hash` doc comment updated to enumerate it). `SAVE_VERSION` 4→5, `1..=5` accepted, old logs replay unaffected (test). Bots never emit byte 16 (asserted). Goldens/xhash/sims/solve all byte-identical to pre-batch-8 baseline (see the T3 numbers below — T1 alone reproduced them, confirming zero leakage from an empty-pool cartridge). **Finding, caught in review**: the brief had assumed §9-C (the pickup register) was pure T2 cartridge-data work, but branching on `kills > spared` at first-pickup time needed an engine-side hook of its own — folded into T1 as a fix round (splitting `PickedUp` into `PickedUpBloody`/`PickedUpMerciful`) rather than deferred, since T2's job is cartridge data, not engine logic.
  - **T2 — cartridge data: the McGuffin's lines wired verbatim** (`b607037`). `src/games/contractor.rs` gained `CARRIED_LINES` (7 `CarryEvent` rows: `PickedUpBloody`/`PickedUpMerciful`/`PickedBackUp`/`StairsUp`/`KillWitnessed`/`SpareWitnessed`/`PutDown`) and `CARRIED_PREAMBLE` (`MCG_001-004`, the fixed six-words-and-interruption intro, printed once at first pickup before the kill/spare branch). FLAVOR-DRAFT-v0's MCG_ lines wired by stable ID, one across-the-board normalization: every em-dash became two ASCII hyphens (`--`), since `render.rs`'s `put_str` is byte-indexed over an ASCII-only font — a raw UTF-8 em-dash would corrupt the grid, not just look wrong, so non-ASCII must never enter a rendered string; no other text, spacing, or capitalization changed (a same-ID text conform, permitted by the flavor doc's own header). **Grounding holds (Rule 5, this task's review job)**: NAR_050-054 held out entirely — all five hardcode "the amulet," true in none of this cartridge's four themed objective names (`objective_name` is "the Quiet Bell"/"the Final Ledger"/"the First Lode"/"the Last Index"), and `carry_event` does no template substitution to fix that; a future reword+`{}`-substitution pass needs its own sign-off before these can wire. MCG_060 and MCG_062 held — both read as bestowing the not-yet-built §9-E mood→light gift; revive verbatim once §9-E lands. MCG_063 held — asserts sparing "the one with the coat," and the coat-monster doesn't exist yet (only the coat-as-item does). `MonsterAdjacent`/`TierCrossed`/`Idle` ship with no authored lines at all (future-content gap, not an oversight; `carry_event`'s empty-pool no-op makes this a safe partial state, same mechanism as batch 7's coat/towel no-give-target rows). **Deviation from the brief**: the brief's `Idle(n)` variant (parameterized by idle-turn count) shipped as a unit `Idle` instead — nothing in this batch's line tables needed the count, and adding an unused parameter now would be speculative; noted in `gamedef.rs`'s doc comment as deferred, not dropped. Sim bots never touch `carry_event`'s dispatch surface directly (it only fires on existing code paths — pickup, climb, put-down, kill/spare bookkeeping — that both bots already exercise), so no bot-policy changes were needed this task; gates re-verified unchanged (see T3 below).
  - **T3 — docs + status (this entry)**: vocabulary 0–15→0–16, `SAVE_VERSION` 4→5, the new CarryEvent doctrine bullet, and the §9 checklist (B/C/D marked done) folded into this file; no source changes.
  - **Verification, batch-final**: byte-identical to the pre-batch-8 (batch-7-T3) baseline on every gate except the expected hash shift — goldens and frame goldens unchanged; `--solve 10000` min615/p50 951/p90 1107/p99 1263/max 1494, worst seed 2108, 0 unwinnable (unchanged); `--sim 5000` greedy 849 wins/4137 combat deaths/14 dark/0 stuck, pacifist 464 wins/4525 combat deaths/11 dark/0 stuck (both **unchanged** from batch 7's numbers — `carry_event`'s dispatch never touches routing/combat/loot decisions, only logs a line, so bot behavior is bit-for-bit identical); `xhash` **`6aa72d29dc62f5bb` → `3bb93aae84ab61fb`** (both backends agree; expected — `speech_attempts` and `objective_dropped` join hashed state, so any save/replay crossing this version boundary correctly produces a different hash). `cargo test`: **106 passed (default/minifb)**, **110 passed (backend-term)** — up from 89/93 pre-batch (new tests cover put-down/pick-up replay determinism, the hashed carry-burn-rate reversion, the `speech_attempts` ladder indexing, and old-save-version acceptance through v5). Sizes: **560,752 stripped / 204,152 packed** (13.84% of the 1,474,560-byte budget) — vs. the pre-batch-8 baseline (555,768/202,704), the batch's net cost is +4,984 stripped / **+1,448 packed**, comfortably inside the brief's ≤+8KB packed gate (90-ish lines of ASCII string data compresses well under UPX, as predicted).
  - **Playtest-pending additions** (alongside the standing lists from batches 2–7): the bare `p` put-down key in both backends (no chord, unlike give/talk/use's existing patterns — does a keystroke with zero modifier read as intentional enough, or does it invite an accidental drop?), and McGuffin line pacing/readability in actual play — the rate limit, the climb-ladder's rung-by-rung escalation feel, and whether the pickup-register's bloody/merciful branch reads as responsive to how the run was actually played.
- **2026-07-21 — batch 9 / the overworld skeleton (story §9-J prep).** Per `docs/superpowers/plans/2026-07-21-batch-9-overworld.md`. Human-picked middle path (Path B): a minimal overworld skeleton — 3 fixed screens, a deterministic screen-link, minimal un-killable trainer/donkey cast — with full NPC substrate, §9-F, and corpse/loot persistence explicitly deferred. Three tasks, all gate-verified green.
  - **T1 — overworld substrate** (`c791c8a`). `WorldId::Overworld` (unit variant, `Game::depth` reused as 1..=3 screen index, exactly the way `Floor` already pins `depth=1` — `Game::stash_level`/`restore_level_at` needed zero changes). `GameDef::overworld: OverworldDef` (3 `OverworldScreenDef` rows, fully authored ASCII, zero RNG, same convention as `AUTHORED_FLOORS`). Three new tiles: `Tile::ScreenLink(bool)` (`=`, deterministic screen-to-screen link, direction derived at parse time from which edge column the glyph sits on, instant walk-onto crossing via `Game::cross_screen_link` — a near-verbatim copy of `descend`'s shape), `Tile::Hole` (`V`, the one true engine-plumbing tile, walk-onto transits `WorldId::Overworld` → `WorldId::Seed(seed)` via `Game::cross_into_dungeon`, reusing the portal forward-transit machinery with a statically-known destination), `Tile::ShutDoor` (`+`, dumb/always-impassable this batch, `+`'s smarter state-dependent behavior explicitly deferred to §9-I). `MonsterDef::passive: bool` (`monsters_act`'s existing `calm`-skip condition gained one `||`) + `MonsterDef::bump: BumpResponse` (`{Fight, Yield, Shove}`, default `Fight`); `TRAINER`/`DONKEY` are the two new un-killable rows. `spend_turn` gained the overworld's no-torch-clock exemption. `Game::new_overworld(seed)` landed as a new, additive constructor — nothing yet called it except new tests and `--dump-overworld` (wiring it into the real front door was explicitly left to T3, SIGN-OFF ASK #3/#4). Acceptance proven byte-identical to the pre-batch-9 (batch-8-T3) baseline on every dungeon-side gate: 5 dump goldens, both frame goldens, `--solve 10000` (min615/p50 951/p90 1107/p99 1263/max1494, worst seed 2108), `--sim 5000` both policies (849/4137/14/0 greedy, 464/4525/11/0 pacifist) — all unchanged, confirming `Game::new`/`gen_level` were provably untouched (not a worldgen MAJOR). Two review-caught fixes folded in: the default screen-1 player-start scan could land on top of the DONKEY's own glyph (both stamp `Tile::Floor`; fixed to require a literal `.` byte), and `cross_screen_link`'s fresh-instantiate branch was missing the `shove_monster_off` collision check the restore branch already had.
  - **T2 — trainer/donkey dialogue** (`45eebae`). `TRAINER_TALK`/`DONKEY_TALK` (`[[&str;2];4]`, the existing 4-bucket talk-line shape — first-landed/mid-landed/threshold-or-calm/failed) wired from FLAVOR-DRAFT-v0's `TRA_001`-`TRA_008`/`DON_001`-`DON_005` verbatim (SIGN-OFF ASK #8: "~5 regard stages" is a content-fit exercise across the existing 8 authored-line slots, not a table-shape widening). `TRAINER`'s `talk_threshold` fix-round-corrected from T1's placeholder value of 2 to the value that makes all four authored stages reachable. **The 3 screens' own map/name/describe strings are NOT part of this task's landed scope** — `OVERWORLD_1`/`OVERWORLD_2`/`OVERWORLD_3` in `contractor.rs` remain T1's MINIMAL VALID PLACEHOLDER content (comments still read "T2: placeholder"/"the real overworld lands in batch 9 T2"), so SPACES-DRAFT-v0.md's actual screen prose is still unconsumed — flagged in the frontier section and §9 checklist above as a future content-only pass, not silently dropped.
  - **T3 — front-door flip + `ref.sav` regen + full gate + this doc** (no gameplay/worldgen changes, no new content). Flipped every real-interactive-session call site from `Game::new` to `Game::new_overworld` (SIGN-OFF ASKS #3/#4): `main.rs`'s fresh-game dispatch (line ~175), `save::replay`'s three reconstruction points (initial + `INPUT_RESTART` + `INPUT_RETRY`), and both backends' End-screen R/N retry/restart handlers (`backend_minifb.rs`, `backend_term.rs`). Left unchanged, deliberately: `headless::solve_seed`/`sim_seed`/`dump`, `backend_term::render_frame_main` (the frame-golden capture surface, doc comment says so explicitly), and the ~110 pre-batch-9 tests that construct `Game::new` directly to assert dungeon-specific facts. Two tests needed real changes because they exercise the front door itself rather than `Game::new` directly: `save_replay_roundtrip`'s `live` fixture switched to `Game::new_overworld` (it must start the same way `replay()` now does, or a shared random-byte script no longer produces comparable state); `echo_records_death_position_after_retry_byte` had to add an explicit overworld→dungeon crossing (S, E, E, E onto screen 1's hole — see below) before its wait-to-death loop, since the overworld's no-torch-clock exemption means waiting there alone can no longer kill the player the way waiting at a direct `Game::new` dungeon start always could. `tests/fixtures/ref.sav` regenerated (seed 4242, log `[S, E, E, E, wait, S]` = bytes `[1,3,3,3,4,1]`): the S/E/E/E prefix is a fixed, seed-independent path from screen 1's default spawn tile onto its `V` hole (verified by direct trace: crosses into `WorldId::Seed(4242)` on the third E), followed by one dungeon wait and one dungeon move to prove real dungeon-side play happened after the crossing, not just the overworld leg. Both backends confirmed to replay `ref.sav` to the identical state (`--replay tests/fixtures/ref.sav` on each binary): `{"hash":"5f4d94c2f9e6aaca", "seed":4242, "turns":6, "depth":1, "hp":20, "light":1998, "dead":false, "won":false}`.
  - **Verification, batch-final** (`make UPX=$(which upx) check`, foreground): `cargo test` **118 passed (default/minifb)**, **122 passed (backend-term)** — up from 106/110 pre-batch (+12 each: the new overworld/passive/bump/front-door tests from T1 plus the two front-door-aware fixture changes above). Dump goldens (seeds 1/2/3/42/1337) and both frame goldens **byte-identical**, confirming `Game::new`/`gen_level` stayed untouched — this is NOT a worldgen MAJOR. `--solve 10000`: min615/p50 951/p90 1107/p99 1263/max1494, worst seed 2108, 0 unwinnable — **unchanged**. `--sim 5000`: greedy 849 wins (17.0%)/4137 combat/14 dark/0 stuck, pacifist 464 wins (9.3%)/4525 combat/11 dark/0 stuck — **both unchanged**, since neither bot's routing ever touches the overworld (they're driven straight through `Game::new`). `xhash`: **`3bb93aae84ab61fb` → `5f4d94c2f9e6aaca`**, both backends agreeing on the new value — expected and correct, exactly the "this is the one gate that should change" case the brief called out (`replay()`'s front door changed AND the new `ref.sav` fixture starts in `WorldId::Overworld`, whose third `hash_world_id` discriminant tag this is the first fixture to actually exercise). Sizes: **569,280 stripped / 206,420 packed** (13.998% of the 1,474,560-byte budget) — vs. the pre-batch-9 baseline (560,752/204,152), the whole batch's net cost is +8,528 stripped / **+2,268 packed**, in line with the brief's 5-10KB packed estimate for T1+T2 combined (T3 added no new content, only a handful of doc-comment-carrying call-site edits).
  - **Playtest-pending additions** (alongside the standing lists from batches 2–8): the overworld itself, start to finish — screen-1 spawn placement next to the donkey, the trainer/donkey's un-killable bump feel (`Yield` swap vs. `Shove` push-or-plant), crossing a screen-link and the hole (both instant walk-onto, no confirming input — does that read as intentional or does it feel like an accidental fall-through?), and retry/restart now landing back at the overworld ("wake up beside the donkey") rather than straight into a fresh dungeon — a real, felt change to the death-and-retry loop that only interactive play can judge.
- **2026-07-22 — batch 10 / the measurement reframe: tactical-bot instrument + measured difficulty baseline.** Per `docs/design/2026-07-22-mercy-economy-arc.md`'s "The measurement reframe" section, which this batch implements in full — a headless-only instrument, ships nothing player-facing. The premise: the existing greedy/pacifist sim bots are a too-dumb reference player, so neither bot's win rate says anything trustworthy about whether the game is actually hard, or about whether violence or diplomacy is the dominant strategy for a competent human. Batch 10 built a competent-player proxy to measure that, for real, before batches 11-12 try to tune the balance it exposed.
  - **The instrument**: two new deterministic policies, `--policy tactical` (tactical-violent) and `--policy tactical-pacifist` (tactical-diplomat) — both zero bot-side RNG, every decision a pure function of `Game` state (asserted by two-run replay-identity tests). Both route AROUND live monsters via a new `tactical_routing_map`, which extends the existing `routing_map`'s wall-stamping to also stamp live monsters as impassable, and both heal (USE the held potion) at ≤2/3 HP. They differ only in what they do when routing around isn't available: `tactical` (violent) attacks, `tactical-pacifist` (diplomat) talks.
  - **A design finding surfaced mid-build, via TDD, and signed off**: a PURE route-around bot — no fallback at all — wins 0/5000. It never commits to a fight or a talk, and wanders until it dies in the dark. Both tactical bots therefore needed an explicit "engage when cornered" rule: when a live threat is unescapably cardinally-adjacent (no route-around move exists), the bot is forced to engage — attack for the violent bot, talk for the diplomat — rather than stalling forever. This rule is why the tactical bots are "competent," not "cowardly," proxies.
  - **The headline measured finding — the concrete proof the batch set out to get**: on the CURRENT, un-tuned game, `--sim 5000 --policy tactical` wins **4163/5000 (83.3%)** and `--sim 5000 --policy tactical-pacifist` wins **2453/5000 (49.1%, kills_total 0)** — against greedy's 17% and plain pacifist's 9.3%. A competent human brute-forces this game; the greedy bot was badly understating how easy it is. And violence (83%) currently dominates diplomacy (49%) by a wide margin — exactly the imbalance batches 11-12 exist to correct (the arc doc's target: violence capped near ~50%, diplomacy the reliably-better-than-50% path).
  - **Frozen invariants — proof this is an instrument, not a MAJOR**: greedy `--sim 5000` (849/4137/14/0) and plain pacifist (464/4525/11/0) both reproduced byte-identical; all 5 dump goldens (seeds 1/2/3/42/1337) and both frame goldens byte-identical; `--solve 10000` unchanged (min615/p50 951/p90 1107/p99 1263/max1494, 0 unwinnable); `xhash 5f4d94c2f9e6aaca` unchanged on both backends. Nothing about worldgen, combat, or the existing bots' behavior moved — the tactical policies are purely additive, observing the same `Game` every other policy already observes. Greedy and plain pacifist stay the floor-of-competence references; their bands must never move when the tactical bands are re-anchored.
  - **New bands, recording (not yet targeting) the baseline**: `tests/tactical-band.json` (`win_pct` in `[75,90]`, `deaths_dark` in `[200,500]`) and `tests/tactical-pacifist-band.json` (`win_pct` in `[40,60]`) — both sized around this batch's measured numbers, not around the arc doc's ~50%/>50% design targets. `make sim` now runs all four policies (greedy, pacifist, tactical, tactical-pacifist), each against its own band file. Batches 11-12 will re-anchor `tactical`'s and `tactical-pacifist`'s bands toward the design targets under their own sign-offs once balance changes land; this batch's job was only to measure and freeze the "before" picture.
  - **Verification**: `cargo test` **123 passed (default/minifb)**, **127 passed (backend-term)** — up from 118/122 pre-batch (+5 each: the two-run determinism tests for both new policies, the cornered-engagement rule, and band-file plumbing). Sizes: **571,352 stripped / 207,348 packed** (14.06% of the 1,474,560-byte budget) — vs. the pre-batch-10 baseline (569,280/206,420), net cost +2,072 stripped / **+928 packed**.
  - **This is a headless, engine-side instrument only** — no player-facing content, no worldgen touch, no title-screen/vocabulary change; the batch's whole point was to measure, not to ship a feature.
- **2026-07-22/23 — batch 11 / the complete ogre + heal scarcity: the combat-balance MAJOR that caps violence.** Per `docs/design/2026-07-22-mercy-economy-arc.md` (§"Batch 11", re-scoped 2026-07-22 to pull the ogre awe-becalm forward from batch 12 so the diplomat is never answerless-against-ogres mid-arc). First use of batch 10's tactical bots as the tuning instrument. Six tasks; an authorized combat-balance MAJOR (all four sim bands re-baselined + dungeon goldens regenerated + `xhash` shifted).
  - **T1 — ogre guaranteed retaliation** (`58908e1` + fix `f822da8`). `MonsterDef.retaliation: i32` (0 everywhere but OGRE): bump-attacking an ogre lands a guaranteed hit back, even on a killing blow, so fighting one always costs HP. Review caught that a lethal retaliation early-returned before `spend_turn`, leaving `turns`/`light` un-advanced unlike every other death path (both are End-screen-visible and hashed); fix folds it through `spend_turn` (turn counted, light+tax burned) while keeping `killer` = the ogre on a same-turn HP+light tie. Runtime-only: goldens byte-identical.
  - **T2 — ogre stand-tall-awe-becalm** (`de406fd` + fix `2832a0d`). Hashed `Monster.awe: u8` + `MonsterDef.awe_threshold: u8` (ogre 3): holding cardinally adjacent without swinging builds awe → becalm via the existing `calm`/`spared` path. **Review found a real bug empirically** (probe test): awe was measured AFTER `monsters_act`'s chase, so a straight-line retreat from a same-speed ogre kept it adjacent and awe-becalmed it *for free, unharmed* — defeating "endure the hit." Fix decides hold-vs-flee from a PRE-chase snapshot against the player's own move (consolidated into one `monsters_act_and_resolve_awe` helper); straight-line retreat now resets awe. `Monster.awe` hashed beside `regard`/`calm`.
  - **T3 — the diplomat's ogre answer** (`a4d3904`). Made redundant by T2 and dropped as a code branch: talk is a no-move action, so the tactical-diplomat's existing cornered-talk already holds ground and awe-becalms an ogre (measured 49%→62% post-T2). A bare "wait to stand tall" branch would be redundant AND worse (portal-transit risk). Added `talking_at_an_ogre_stands_tall_and_awes` proving the composition.
  - **T4+T5 — heal scarcity + tune to violence ~50% + re-baseline all four bands** (`e98a971`, the MAJOR). Tuning against the tactical bots landed competent violence at **46.8%** (target [45,55]). Final values (contractor.rs): OGRE `retaliation` 6, `awe_threshold` 3, `loot_count_per_depth` 1 (was 2 — the golden-changing lever), potion `UseEffect::Heal` 3 (was 8), `hp_gain_per_depth` 3 (was 4). **Finding for future tuners:** cutting the potion-vs-sword DRAW rate (`loot_potion_num/den`) BACKFIRES — a non-potion slot rolls a SWORD, so fewer potions just adds offense (tactical went UP 76%→80%); it stayed 3/4, and real heal scarcity came from fewer total items + smaller heals. Raising retaliation alone has diminishing returns (the bot routes around / awe-spares ogres). Goldens regenerated: **item-content-only** (fewer deep-floor items; monster counts + all geometry byte-identical; depth-1 unchanged since per-depth adds 0 there, so frame goldens held).
  - **T6 — full gate + docs (this entry).** Measured @ `--sim 5000`: tactical-violent **46.8%** (2339), tactical-diplomat **27.5%** (1373, kills 0), greedy **0.7%** (33), pacifist **0.3%** (16) — all inside re-baselined bands. Two tests updated for the new balance (`potion_use_applies_authored_heal` now reads the heal amount from GAME; `sim_bot_wins_some` switched greedy→tactical since greedy can no longer win 1-of-50). `--solve 10000` **unchanged** (min615/p50 951/p90 1107/p99 1263/max1494, 0 unwinnable — heal/loot changes don't touch reachability). `xhash` **`5f4d94c2f9e6aaca` → `697c9355d21fe67e`** (both backends agree; `Monster.awe` joined the hash). `cargo test` **129 passed (default/minifb)**, **133 (backend-term)**. Sizes: **573,424 stripped / 208,220 packed** (14.12% of budget), +2,072/+872 vs batch 10.
  - **The landing, stated plainly:** the game is now HARD — competent violent play is a coin flip (46.8%), and dumb play (greedy/pacifist) wins <1%. Diplomacy (27.5%) is currently the HARDER road, BELOW violence — the deliberate interim: the diplomat endures ogre hits to awe them at real HP cost with no payoff yet, and **batch 12's light-as-grace (spare → light) is exactly what flips diplomacy above violence.** Human-signed-off 2026-07-22/23.
  - **Playtest-pending additions** (alongside batches 2–10): the ogre itself — does eating a guaranteed hit to fight one, or holding your nerve through several to awe one, read the way the numbers say? Whether "the game got hard" feels fair or punishing at a human's hands (the bots say coin-flip-violence / harder-mercy); and whether standing tall against an ogre (holding while it pummels you) is legible as a deliberate act rather than indecision.
- **2026-07-24 — batch 12 / light as grace: the second lantern — THE FLIP (diplomacy above violence).** Per `docs/design/2026-07-22-mercy-economy-arc.md`'s "Engine 2 — light as grace" and `docs/superpowers/plans/2026-07-24-batch-12-light-as-grace.md`. The mercy-economy arc's keystone: the batch that made mercy the *reliable* path, above violence, for the first time. **The redesign that got there.** The batch opened as "spare → light" (T1 kills-dim-your-torch `3cdee5e`, T2 spares-feed-your-torch-capped `3da64bc`), but measurement showed that couldn't flip anything: the diplomat was HP-limited (batch-11 heal scarcity), not light-limited — it was drowning in unspent light (winning runs ended with ~1259 light in hand), so paying it *more* light changed nothing. The human delivered a full redesign (the "second lantern" spec, `9e97aa7` plan rewrite + `fc5f050` arc-doc amend) that superseded the original T3–T5: the McGuffin becomes a POSITIONAL light source whose radius is her verdict on your kill/spare record, and REST heals the HP that was the real ceiling. Executed as R1–R8.
  - **R1 — third strike: pin 1.75 + kill the recurring E0716** (`d2ce9fc`). The temporary-dropped-while-borrowed slip bit a third time; **root cause finally found: the local toolchain is `rustc 1.97.1`, not the 1.75 this file long claimed** (a stale doctrine line, now corrected), so E0716 never reproduced locally. Fixed the offending band-key match's temporary, pinned `rust-version = "1.75"` in `Cargo.toml`, and added a `make msrv` step (folded into `make check`) that installs+runs a real 1.75 when rustup exists and otherwise warns loudly that the ambient toolchain does NOT enforce the floor — CI must.
  - **R2 — strip the spare-light stipend, keep `record_spare`** (`63ad5df`). Removed T2's per-spare light gain (the redesign pays mercy at the TOP via her shine, not per-spare mid-run); `record_spare` stays as the single consolidated becalm site (now just `self.spared += 1`), which R4's mood mechanic then hooks.
  - **R3 — rest: waiting while hurt heals slowly** (`78942c6` + fix `7725d65`). A wait (byte 4) while `hp < maxhp` and NOT adjacent to a live non-calm monster heals `rest_heal` (1) HP; both tactical bots got a rest branch (`REST_LIGHT_RESERVE`/`STALL_LIMIT`/`MAX_REST_TURNS_PER_RUN` guards). **Review-caught bug:** the gate first used CARDINAL adjacency, but monsters attack from CHEBYSHEV (incl. diagonals), so a diagonally-parked attacker gave a free heal every turn — reproduced (hp 15→14 not 13), fixed to Chebyshev in both `rest_heal` and the bot mirror.
  - **R4 — the pickup verdict: mood anchored to your record** (`7e92912`, formula human-signed-off). At first pickup, mood SEEDS from the descent's kill/spare record as a running-average anchor (`mood_sum = mood_anchor_weight * anchor_score; mood_count = mood_anchor_weight`, weight 20); `mood()` averages every post-pickup kill (`kill_valence`, graduated rat 0/goblin 15/ogre 30 — **human correction: "not every kill is as despicable"**) and spare (`mood_spare_valence` 100) in after. `mood_sum`/`mood_count` hashed → `SAVE_VERSION` 5→6, `parse_save` `1..=6`. **Human corrections during design:** mood is a running average (not additive/ceiling/blend), and it ANCHORS at pickup with climb acts averaging against it.
  - **R5 — she is a light source; death rewritten** (`7bc88e1`, own review). `Game::mcguffin_light() -> Option<((i32,i32),i32)>` (radius via `mood_shine_radius`, `mood_shine_tiers [(75,6),(50,4),(25,2),(MIN,0)]`, dark tier pinned 0); `compute_fov` composes torch+her circles via the shared `light_circle`; the darkness death became `if light <= 0 && !lit_by_mcguffin()`, so a merciful player can finish inside her light after the torch dies. New presentation-only `died_out_of_her_light` (End-screen message only, joins the exclusion set).
  - **R6 — tune + re-baseline four bands: THE FLIP** (`66da2c7` stuck-fix + `0594522` re-baseline). Landed the flip on the STARTING [TUNE] values, no tuning iteration: **tactical-pacifist (diplomat) 59.7% (2985) > tactical (violent) 43.7% (2185)**, mechanism in two numbers — `deaths_dark` 14 (diplomat, lit home) vs 762 (brute, dark). Bands re-baselined: tactical `win_pct [37,50]`/`deaths_dark [550,1000]`, tactical-pacifist `win_pct [51,66]` (the 66 ceiling is the design's own "too generous" guard). Greedy/pacifist unmoved (<1%, they never carry). **Stuck-fix:** 5 stuck tactical-pacifist runs surfaced a latent batch-10 `tactical_routing_map` myopic-replanning oscillation (previously masked by dark-death, exposed by R5's shine) — fixed with a `STALL_LIMIT` fallback to static routing.
  - **R7 — mood/rest lines + depth-5 telegraphing** (`eb24105` + grounding fix `3f34abd`). `CarryEvent::RestedBright`/`RestedDim` (mood-keyed by her visible shine radius, dispatched from `wait_turn` on a mending rest); the trainer's stage-0 variant-1 line now warns "what's below keeps a tally" (the guaranteed pre-pickup telegraph that she judges the descent; full trainer-memory deferred to batch 13). Scope call (documented): the `StairsUp` ascent ladder is NOT split — its `speech_attempts` index is load-bearing. **Per-task grounding review caught a real overclaim:** MCG_060 ("a little of mine") was revived into RestedDim, but that pool also fires at radius 0 (mood<25) where she casts NO light — so MCG_060 went back to held and RestedDim now carries two lines truthful at radius 0 AND 2. Gate-neutral (carry_event only touches `flavor_rng`+the log): tactical-pacifist reproduced byte-identical (2985/2001/14/0); a new end-to-end dispatch test proves the feature actually fires.
  - **R8 — full gate + docs (this entry).** Batch-final @ `--sim 5000`: greedy **0.6%** (31), pacifist **0.3%** (17), tactical **43.7%** (2185, `deaths_dark` 762), tactical-pacifist **59.7%** (2985, `deaths_dark` 14) — all inside the four re-baselined bands. `--solve 10000` **unchanged** (min615/p50 951/p90 1107/p99 1263/max1494, worstSeed 2108, 0 unwinnable — her shine only ADDS light and the solver ignores her, so winnability is untouched). Dump+frame goldens **byte-identical** (not a worldgen MAJOR). `xhash` **`697c9355d21fe67e` → `cf58eeadef7a31be`** (both backends agree; `mood_sum`/`mood_count` joined the hash at R4, SAVE_VERSION 6 — the committed `ref.sav` never carries the McGuffin, so the shift is purely the wider hashed-state shape, not a mood change). `cargo test` **153 passed (default/minifb)**, **157 (backend-term)** — up from 129/133, +24 covering rest-heal (Chebyshev gate), the pickup anchor, the mood running-average, `mcguffin_light`/`lit_by_mcguffin`/the death rewrite, SAVE_VERSION-6 acceptance, and the R7 rest-line dispatch. Sizes: **576,168 stripped / 209,152 packed** (14.18% of the 1,474,560-byte budget; +2,744 / +932 vs batch 11). The `msrv` gate warns-and-passes here (no rustup → ambient 1.97.1), flagging that CI must run the real pinned 1.75.
  - **The landing, stated plainly:** the arc's thesis is now true in the gate — a merciful descent is the RELIABLE way home, because the McGuffin you carry lights the climb exactly in proportion to how gently you reached her, and a torch-dead brute is left in the dark he made. Diplomacy 59.7% > violence 43.7%, on the first measurement of the redesigned system.
  - **Playtest-pending additions** (alongside batches 2–11): does the verdict-at-pickup read as judgment or ambush; does resting feel like being tended (and is the rest-heal pace right); does the brute's cheese-lit / dark-McGuffin climb land as tragicomic rather than just unfair; do the mood-keyed rest lines and the trainer's tally-warning land in play; and — the whole bet — does a human actually *feel* mercy become the easier road on the way up, or just get told so by the bots.
- **2026-07-24 — batch 13 / the mercy-economy ride-alongs: the becalm toolkit gets texture; the flip holds through harder diplomacy.** Per `docs/design/2026-07-22-mercy-economy-arc.md` (the ride-alongs the batch-12 redesign split out of the arc's original "batch 12" bundle) and `docs/superpowers/plans/2026-07-24-batch-13-ride-alongs.md`. Eight tasks, all gate-verified. The arc's central "diplomacy must be harder" thesis lands: mercy stops being spam-talk and becomes per-creature nerve. **Combat-balance sign-off class** (like batch 11 — moves the four bands, regenerates nothing in worldgen: goldens+frames byte-identical throughout).
  - **T1 — the trainer reads your last life** (`39a4827`). Presentation-only `last_life_bloody: Option<bool>` (+ a once-per-life `last_life_greeting_spoken`), set at death from `kills > spared` and forwarded across `INPUT_RETRY` exactly like `echo` (captured off the dying game before it's replaced); both join the presentation-only exclusion set, NO `SAVE_VERSION` bump. The overworld trainer greets a fresh post-death life via a new generic `MonsterDef::resurrection_lines` (only the trainer populates it): bloody → `TRA_007` verbatim ("Back already? Happens. I don't ask. You don't ask."), merciful → a grounded new counterpart. The irony is baked in — the only way back to him is the death his "kill five rats" advice caused. Gate-neutral (158 tests, goldens identical). **Implementer stalled before committing; I verified + committed during recovery** (the harness auto-backgrounds long runs and the subagent yields — the recurring pattern; T1's own late self-check independently confirmed `xhash` byte-identical).
  - **T2 — cheese → goblin** (`f1cd336`). `GiveRule::stay_and_roll`: cheese ALWAYS stays the goblin (guaranteed tempo, reusing the batch-5 transient stay via `monsters_act_and_resolve_awe(Some(mi), …)`) AND rolls `receptivity` off `parley_rng` for an outright becalm (gambled grace, feeds light via `record_spare` + `SpareWitnessed`). Cheese→rat keeps the batch-7 PENALTY; ogres get no row. The human's positive cheese give-target (story §12.14). Sim-neutral (bots never GIVE): 163 tests, goldens identical, grep-clean.
  - **T3 — becalm return-trip dividend** (`b80f3f1`). `BalanceDef::becalm_dividend` refunds a trickle of light the first time you pass a becalmed monster (hashed `Monster.dividend_paid` guard → `SAVE_VERSION` 6→7, `parse_save` `1..=7`, v1–v6 all replay byte-identical). Kept SMALL (value 2) per the arc GUARD (§215: this is the exact lever that made pacifism dominant in batch 5). Measured INERT at 2 (bit-identical sim; the diplomat's deaths are combat not dark, so light barely moves its win-rate) — kept as a flavor touch, not a balance lever. 167 tests, goldens identical, solve unchanged.
  - **T4 — potion is mammal-medicine** (`77bf789`). `GiveRule::enrage`; the blanket potion→any row replaced with per-kind biology-coded rows: potion→rat heals-to-full + becalms (the most self-sacrificing mercy); potion→goblin/ogre ENRAGES (regard crash + a free monster swing via the failed-talk hit path, NO damage to the monster, not tied to player ATK — a pure read-the-bestiary trap, no ranged-violence loophole). Sim-neutral (bots never GIVE): confirmed byte-identical (2985/14 at that point), 170 tests, goldens identical.
  - **T5 — goblin awe + the paired lethal punish-behaviors** (`6331b71`, the batch's keystone new mechanic). The goblin is the MIRROR of the ogre: becalm by GIVING GROUND (was toe-to-toe, stepped away — "walk backward while talking") vs the ogre's HOLD; each punishes the opposite move (`MonsterDef::awe_by_giving_ground`/`punish_wrong_move`) with the monster's own swing (`killer` attributed on a lethal one). Reuses batch 11's PRE-CHASE-snapshot discipline exactly. **The implementer found and fixed THREE real bugs via sim measurement** (not review): `held_adjacent` needed `old_dist == 1` (a fresh approach isn't "planted") AND `(px,py) == prev_player` (only STANDING STILL is planted, not corridor traffic near a goblin); the bot's give-ground draft cratered the diplomat before landing on a narrow cornered-only occupancy-checked fallback. The tactical-diplomat bot was extended to read the creature and pick the opposite correct move. Comprehensive tests incl. the batch-11-style probe (`straight_retreat_from_goblin_builds_awe_despite_post_chase_adjacency`), mutually-fatal, and lethal-punish-`killer` (176/180 tests). Sim-moving (measured for T7); goldens identical, `xhash` unchanged (`Monster.awe` already hashed, no new field). **Design reading (documented):** goblin talk-becalm stays active alongside awe/cheese — the `punish_holding` mechanic achieves the arc's "not spam-talk" (planted talk is punished; the safe way to talk a goblin is exactly the walk-backward awe move), and the measured diplomat was BELOW target, so keeping talk as a route helps T7 reach target rather than hurting.
  - **T6 — the donkey-follow seed** (`642b07e`, rung 2 of 3). `MonsterDef::follows_when_calm`: schmooze the donkey to its top `DONKEY_TALK` rung (calm) and it trails you around the overworld and across screen-links (`Game::take_calm_follower` carries it across a `=`), but WON'T descend the hole — "he only goes down for someone he loves." Rung 3 (LOVE → descend → ogre-becalm-by-presence → the secretly-super reveal) is a later companion batch, NOT built. Overworld-only, sim-neutral (bots never enter the overworld): 181/185 tests, goldens+frames identical, no `SAVE_VERSION` bump (reused `calm` + position).
  - **T7 — tune + re-baseline four bands** (`295eb30`, the combat-balance closeout). ONE measured lever — goblin `awe_threshold` 3→2 — lifted the diplomat from 46.0% back into the arc's 50-60 target. Final `--sim 5000` (make check GREEN, all stuck=0): **tactical-pacifist (diplomat) 51.0%** (2549, 14 dark) → band `[45,60]`; **tactical (violent) 33.9%** (1695, 548 dark) → band `[27,42]`/`[400,800]`; greedy 60 (1.2%), pacifist 27 (0.5%) → floor bands `[0,4]` unchanged (shifted from the goblin mechanic touching all bots; comments refreshed). **THE FLIP HELD with a 17-point margin.** Violence being doomed at ~34% is on-theme — the goblin mechanic was NOT weakened to lift it. Measured against the tactical bots, never hand-tuned to feel (the arc's iron discipline). **Implementer stalled on the long sim cycles; I did T7 inline** (tuning is judgment work anyway).
  - **T8 — docs (this entry)** + the arc-doc roadmap renumber (the ride-alongs are batch 13, portal ROI becomes batch 14 — the 2026-07-24 redesign split).
  - **Verification, batch-final** (`make check` GREEN): `cargo test` **181 (default/minifb) / 185 (backend-term)**, up from 153/157. Dump+frame goldens **byte-identical** (NOT a worldgen MAJOR). `--solve 10000` **unchanged** (min615/p50 951/p90 1107/p99 1263/max1494, 0 unwinnable — none of these mechanics touch reachability). `xhash` **`cf58eeadef7a31be` → `ca6604617a55903a`** (both backends agree; shifted at T3 when `Monster.dividend_paid` joined the hash, SAVE_VERSION 7). Four sim bands re-baselined (T7). Sizes: **581,832 stripped / 211,016 packed** (14.31% of the 1,474,560-byte budget) — +5,664 / +1,864 vs batch 12.
  - **The landing, stated plainly:** diplomacy is now a per-creature skill, not a talk-spam — and it is still the reliable road home (51%), clearly above doomed violence (34%), with dumb play losing outright (<2%). The whole mercy economy reads end to end: kills dim the McGuffin's light and her verdict darkens your climb; mercy — the RIGHT mercy for each creature — lights the way and she carries you home.
  - **Playtest-pending additions** (alongside batches 2–12): do the goblin/ogre awe TELLS read clearly enough that a wrong-move death is learn-by-death rather than a gotcha; does "walk backward while talking" vs "stand tall" feel like a real per-creature read under pressure; does the potion-on-a-goblinoid trap teach rather than just punish; does the cheese fork (burn for light vs bribe a goblin) feel like a real choice; and does the donkey following you around the field, then refusing the hole, land the "he's scared / stuck" misdirection the rung-3 reveal will later overturn.
- **2026-07-24/25 — batch 14 / portal ROI + the render fix that makes her shine visible.** Per `docs/superpowers/plans/2026-07-24-batch-14-portal-roi.md` (human sign-off 2026-07-24: the authored-floors MVP, NOT a worldgen MAJOR, with three required additions). Portals get a payoff at last — a light-cache pilgrimage — and a playtest bug that hid her whole visual payoff gets fixed. Five portal-ROI tasks + a render fix, all gate-verified.
  - **T1 — the light-cache item** (`aca835f`). `ItemEffect::LightCache(i32)`, `LIGHT_CACHE` (glyph `$`) — a walk-over `Consume` item that refills the light pool, placed only in authored-floor portal destinations (root goldens byte-identical). 183 tests.
  - **T2 — derive cache values from a geometric dive-cost probe** (`496f91d`, addition #1). `--probe-floors`: dive cost = `2 × bfs_dist(entry `<` → cache)` per authored floor (12 and 14), cache value = worse-floor cost + 50% margin = **21** (+EV on both), documented `START_LIGHT`-style, reproducible via the flag. Caches placed in both floors. 187 tests, root goldens byte-identical.
  - **T3 — the anti-brute-bailout cap** (`ec8d9b7`, addition #2). `BalanceDef::max_cache_light_per_run = 60`, sized from a slaughter route's self-burn (`kill_light_penalty 8 × tactical ~17 kills/run ≈ 135`) so caches can't rescue a brute; hashed `Game.cache_light_collected` → `SAVE_VERSION` 7→8, `parse_save` `1..=8`, v1–v7 replay byte-identical. Doesn't bind today (2×21=42 < 60) — a forward guard. Sim-neutral (bots don't dive): tactical-pacifist byte-identical 2549/14. 191 tests.
  - **T4 — the grounded threshold telegraph** (`607b157`, addition #3). `Game::portal_describe`'s `Dest::Floor` arm previews a cache ONLY when `map_has_cache_reward(floor.map)` proves the floor holds one — reads the cache glyph from the item def (engine grep-clean), never promises a barren floor's cache. Root goldens byte-identical, sim-neutral. 194 tests.
  - **Render fix — grade each cell by its own light source** (`99e8203`, a playtest bug, human-specified fix). `compute_fov`/`light_circle` correctly raycast the McGuffin's mood-sized shine as an independent circle and the death check honors it, but `render_play` still graded the whole frame by one `light_pct(fov_radius(g.light))` (a batch-3 one-light-source leftover), so tiles only SHE lit rendered at the dying torch's murk and her light read as torch-capped. Fix: presentation-only per-tile `Game.lit_r: Vec<u8>` (a derived buffer like `vis`, NEVER hashed/saved — `xhash` unchanged, no `SAVE_VERSION` bump), filled by `light_circle` with the max source radius (brightest wins on overlap); `render_play` + `scene()` grade every cell by `light_pct(lit_r[i])`. Turn-0 frame goldens BYTE-IDENTICAL (at full torch it's a provable no-op) — which is exactly why every gate stayed green until a human carried her. Guarded by the `mcguffin_shine_renders_at_her_brightness_not_the_dying_torch` unit test, which constructs the dim-torch + wide-shine state directly. **Finding:** a from-replay frame golden was built (the infra worked — `--render-frame --load`, a merciful-descent save generator) but rejected — a MEANINGFUL one needs her shine radius > the torch radius, which requires burning ~950+ light while carrying (~475 turns), a late-game state an efficient bot wins or dies before reaching, so not naturally reproducible via a bot replay (the unit test is the right guard). 192 tests at that commit.
  - **T5 — docs (this entry)** + the portal-ROI/render-fix doctrine bullets, `SAVE_VERSION` 8, the frontier, and the §9/roadmap notes.
  - **Verification, batch-final** (`make check` GREEN): `cargo test` **194 (default/minifb) / 198 (backend-term)** (up from 181/185). Dump+frame goldens **byte-identical** (NOT a worldgen MAJOR — the reward lives only in authored-floor destinations, the telegraph is a log line, and the render fix is a no-op at turn 0). `--solve 10000` **unchanged** (min615/p50 951/p90 1107/p99 1263/max1494, 0 unwinnable). All four `--sim 5000` bands **UNCHANGED** (greedy 60/pacifist 27/tactical 1695/tactical-pacifist 2549/14 — bots never transit portals, so portal ROI is human-facing content, not a sim lever). `xhash` **`ca6604617a55903a` → `9dc61f24290c995a`** (shifted at T3 when `cache_light_collected` joined the hash, SAVE_VERSION 8; the render fix's `lit_r` is NOT hashed and shifts nothing). Sizes: **584,832 stripped / 212,112 packed** (14.38% of the 1,474,560-byte budget).
  - **The landing, stated plainly:** the multiverse is now a real choice for the light-rich — dive a portal, read the telegraph, bank a cache the dive-cost probe proves is +EV — and a brute can't cache-farm his way out of the light-as-grace punishment. And the whole point of light-as-grace is finally VISIBLE: a merciful player carrying her home walks in her light, not the torch's murk.
  - **Playtest-pending additions** (alongside batches 2–13): does the portal threshold telegraph make the dive decision legible; does a light-rich pacifist actually feel the pilgrimage pay off while a brute can't afford it; and — the render fix — does her shine now READ on screen as a second, warmer light that widens with mercy (the thing the whole arc was building toward, invisible until now).
- **2026-07-25 — batch 15 / two playtest refinements: goblin awe earned, item-use chosen.** Direct human playtest feedback. Two fixes, both gate-verified; neither a worldgen MAJOR (root goldens byte-identical).
  - **T1 — goblin awe is earned by talking while giving ground, and dies once it has struck you** (`3b5345a`). The batch-13 mechanic awed a goblin on a bare SILENT give-ground move — a goblin you're merely fleeing, or one that already hit you, shouldn't be pacified. Fix: a give-ground move only ARMS hashed `Monster.yielded`; awe BUILDS on a subsequent TALK (`resolve_awe` gained a `talked` param) when `yielded && !struck_player`, consuming `yielded` — the two-beat retreat-then-talk rhythm. New hashed `Monster.struck_player` (set on ANY monster hit) permanently blocks give-ground awe. Talking is composure, not planting (excluded from `held_adjacent`, else a no-move talk reads as "planted" and gets punished — required for the mechanic to work at all). Ogre untouched (`struck_player` gates only give-ground kinds; enduring its hit IS its awe). `SAVE_VERSION` 8→9, `parse_save` `1..=9`, v8 replays byte-identical. The tactical-diplomat bot learned the rhythm. **Combat-band-moving:** the flip HELD but narrowed — diplomat 51.0%→**36.2%** (1811), violent **32.3%** (1617, structurally untouched — never talks/gives ground), greedy 15/pacifist 10, all stuck=0. Bands re-baselined (`tactical-pacifist` `[25,50]`, `tactical` `[27,42]`, floors unchanged). 10 new tests. **Watch-point flagged to the human:** the ~4-point flip margin is thin; goblin `awe_threshold` (2) is the restore lever, tuned against the bots if wanted.
  - **T2 — item-use selector + held counts** (`0804e7a`). USE (byte 15) used to blindly apply `held.last()`. Now `Game::use_item_kind(kind)` applies the top-most held item of a chosen kind (graceful no-op otherwise), reached by the first input-vocabulary growth past 16: bytes `17 + kind` = "use one of that kind" (byte 15 stays as use-top). `SAVE_VERSION` 9→10 (a v1..=9 log can't contain a 17+ byte, so older saves replay byte-identical; v9→v10 test). No new hashed STATE (`held` was already hashed; the new bytes just choose which held item to consume). `Game::held_summary()` (derived) groups held by kind with counts. Both backends got a `u`-key use-selector: a one-line menu of held items by GLYPH + count (`Use: 1:o x2  2:~ x1`), digit keys emit `17+kind`; the armed state is frontend-local chrome like the talk/give chords (engine grep-clean — items named by glyph). Sim-neutral (bots keep using byte 15): root+frame goldens byte-identical, 208 tests. **The selector overlay is interactive-only — NEEDS HUMAN PLAYTEST.**
  - **T3 — docs (this entry)** + the goblin-awe and item-use doctrine bullets, the input-vocabulary `17+kind` range, `SAVE_VERSION` 10, the frontier.
  - **Verification, batch-final** (`make check` GREEN): `cargo test` **208 (default/minifb) / 212 (backend-term)** (up from 194/198). Dump+frame goldens **byte-identical** (neither fix touches worldgen or the turn-0 render). `--solve 10000` **unchanged** (0 unwinnable). `--sim 5000`: the tactical bands MOVED (T1's harder goblins) — greedy 15/pacifist 10 (floors, `[0,4]`), tactical **1617 (32.3%)** `[27,42]`, tactical-pacifist **1811 (36.2%)** `[25,50]` — the flip holds, all stuck=0; the item-use fix is sim-neutral. `xhash` **`9dc61f24290c995a` → `c8323b3b2e1ca28a`** (shifted at T1 when `struck_player`/`yielded` joined the hash, SAVE_VERSION 9). Sizes: **588,992 stripped / 213,008 packed** (14.45% of the 1,474,560-byte budget).
  - **Playtest-pending additions** (alongside batches 2–14): does the two-beat goblin awe (retreat, talk, retreat, talk) feel like a real nervy diplomacy skill rather than a chore, and is the ~4-point flip margin acceptable in a human's hands or does diplomacy now feel too punishing; and does the item-use selector (the `u`-key menu with counts, digit to pick) read cleanly in both backends.
- **2026-07-26 — batch 16 / the flip becomes a make-check invariant: relational gate + `awe_threshold` restore + doctrine.** A playtest-driven follow-on to batch 15's thin margin, human-ratified as "fix the metric, not just the number" (see the arc doc's "Ratification (2026-07-25)" section and the new plan of record `docs/design/2026-07-26-road-to-1.0.md`). Batch 15's goblin fix was CORRECT but dropped the diplomat to 36.2% — a ~4-pt margin over violent 32.3% resting on two OVERLAPPING absolute bands, so the flip could silently invert with every gate green. Two-front fix, plus the doctrine the episode earned:
  - **The relational flip gate** (`--sim-flip N`, `headless::sim_flip_main` + the `band_scalar` helper; `844ea67`): runs the tactical and tactical-pacifist bots and asserts `diplomat_win% ≥ violent_win% + flip_margin`, exit 1 on violation. `flip_margin` (3) and `flip_n` (5000) are new fields in `tests/tactical-pacifist-band.json`; margin 3 ≈ 3σ on the difference of two ~35% binomials at N=5000 (each proportion's SEM ~0.7pt → the difference's ~1pt) — beyond sim noise, not a coin-flip cushion. Wired into `make check` as the `flip` step, AFTER `sim` (each per-policy band must be green before the relational check is meaningful). The diplomat band's floor is ALSO ratified as a thesis-bearing absolute (30, same band file) — the PAIR (relation + floor) excludes both inversion AND the "both routes lose, diplomacy merely least-bad" world. Two new tests: `band_scalar_parses_flip_fields` and `flip_margin_comparison_is_correct` (the latter proves the pass/fail arithmetic including the inverted-order and thin-margin FAIL cases).
  - **The `awe_threshold` 2→1 restore** (goblin, `contractor.rs`, `844ea67`, human pre-ratified): the surgical lever — the CORRECT diplomat gesture (give ground, then talk while still armed) now pays off in one beat instead of two — lifting the diplomat 36.2%→39.0% over unchanged violent 32.3% (measured margin 6.7). NOT a goblin-receptivity change (rejected as too blunt: receptivity moves all three kinds' talk at once).
  - **Why the RELATION, not the absolute >50% target** (the Goodhart catch, ratified through human sign-off): the tactical-pacifist bot models neither cheese→goblin nor potion→rat — whole diplomatic routes — so its absolute number UNDERSTATES human diplomacy; the >50% was a proxy that drifted as the game's toolkit outgrew the bot that doesn't use it. The violent bot ≈ a complete human violent route, so the bot-relation is CONSERVATIVE — the gate holds a-fortiori between humans. Expiry condition (documented in the arc ratification): a batch that adds violent-route toolkit the bot can't model. The item-using diplomat bot is priced instrument debt whose landing re-opens the absolute-target question.
  - **Doctrine promoted, born of this episode** (committed with this batch's docs): the **instrument-parity rule** — a batch that adds a *move to a route* teaches the route's bot the move IN THE SAME BATCH, or the band measurement is an artifact (generalized from batch 15's goblin-awe episode; every §9-F diplomacy minigame is a fresh instance) — and **read-canon-before-pronouncing** — a scope call must cite the human-locked doc, not memory (the §9-F double-mis-scope as the worked example). The **MSRV note corrected**: this container has rustup with `1.75.0` as its active DEFAULT, so `make check` here GENUINELY enforces the floor (batch 16 closed on a real 1.75 gate); the `/bin/sh`-lacks-`~/.cargo/env`-PATH gotcha and the size-is-toolchain-dependent caveat are recorded there too.
  - **Verification, batch-final** (`make check` GREEN end-to-end under real `1.75.0`, exit 0): `cargo test` **210 (default/minifb) / 214 (backend-term)**, up from 208/212 (+2 flip tests). Dump+frame goldens **byte-identical**; `xhash` **`c8323b3b2e1ca28a` UNCHANGED** from batch 15 (batch 16 adds NO hashed state — the flip gate is pure headless measurement and `awe_threshold` is a `MonsterDef` const; the short `ref.sav` session hits no goblin-awe resolution). `--solve 10000` **unchanged** (min615/p50 951/p90 1107/p99 1263/max1494, worstSeed 2108, 0 unwinnable — `awe_threshold` doesn't touch worldgen/reachability). `--sim 5000`: greedy **15 (0.3%)/7 dark**, pacifist **10 (0.2%)/5 dark**, tactical **1617 (32.3%)/618 dark**, tactical-pacifist **1951 (39.0%)/13 dark** — all in-band, stuck=0; **`--sim-flip 5000`: margin 6.7 ≥ 3, PASS**. `SAVE_VERSION` stays **10** (no hashed-state-shape or vocabulary change). Sizes under real 1.75.0: **665,968 stripped / 219,776 packed** (14.9% of the 1,474,560-byte budget) — NOT comparable to batch 15's 588,992/213,008, which were measured under the newer ambient toolchain (a toolchain delta, not code bloat: batch 16 added one headless function, one Makefile step, two json fields). Commits: `844ea67` (gate + `awe_threshold`), `87d62bd` (arc-doc ratification), + this docs commit.
  - **The landing, stated plainly:** the mercy arc's central thesis — diplomacy is the reliable road home, above violence — is no longer a hope the overlapping bands merely imply; it is an invariant `make check` OWNS and fails loudly on. And batch 16 SEALS the instrument era: from here the work is the game itself — the §9-F cast, the ending-as-exam, polish/legibility, ship — per `docs/design/2026-07-26-road-to-1.0.md`. The net is up; next come the acrobats.
- **2026-07-27 — batch 17 / the mimic: cast batch one + the NPC-vault worldgen MAJOR.** The first of the road-to-1.0 cast arc (`docs/design/2026-07-26-road-to-1.0.md`), built from the human-signed-off package `docs/superpowers/plans/2026-07-27-cast-npc-vault-major.md` and the plan `docs/superpowers/plans/2026-07-27-mimic-batch.md`. Six tasks, subagent-driven, controller-reviewed. The mimic (story §9-F D3) ships: disguised furniture until you approach, then courtship-as-hunting, with THE POLITE NO as its mercy route. Carries the talk-minigame FRAMEWORK the mantel (§9-I) will later reuse, the sword→Hold + set-down-for-regard mechanic, and the guaranteed cast-vault placement (THE STAGE on D5, mimic room on D3).
  - **T1 — the worldgen MAJOR** (`4ea78bf`, `feat!`). `GameDef::required_vaults` + `place_required_vault_room` (an EXHAUSTIVE two-tier placement scan — the optional path's bounded 40-try retry silently missed ~5% of seeds; the exhaustive scan is a real guarantee, test-covered over 0..2000): a ROOT cast depth stamps its required vault UNCONDITIONALLY on the `vault` channel before the optional `vr.chance(2,5)` roll; every other depth (non-cast root, all portal-world depths) keeps the original path BYTE-FOR-BYTE (per-depth vault channel → D1/D2/D4 goldens byte-identical). The objective relocated from the deepest-BFS push to THE STAGE's `&` pedestal (exactly one objective; the old push is an intentional empty arm). New solver-invariant test (every seed: STAGE on D5, mimic on D3, exit reachable) + the amendment-C dump-test (cast glyphs at canon depths in the golden seeds — survives regen, unlike an eye-check). `start_light` re-derived (worst-case round-trip 1494→**1476**, worst seed 82; `start_light` 2000 unchanged, solver-band still in range). Goldens regenerated ONCE (diff isolated to D3/D5). Mimic glyph **`c`** (chest; a cartridge fact — human-confirm at playtest). Not a save-shape change; xhash unmoved (`ref.sav` only reaches depth 1).
  - **T2 — sword→Hold + set-down + put-down kind-routing** (`66551f7` + fix `ac60254`). SWORD `on_pickup` `Consume`→`Hold`; `g.atk` now DERIVED (`recompute_atk` = `starting_atk` + Σ held `AtkBonus`, called at every `held` mutation) — a set-down sword's bonus can't linger. Set-down-for-regard (`ItemDef::disarm_regard`, sword `[TUNE 3]`): setting the sword down adjacent to a monster banks regard, GUARDED once-per-monster by the hashed `Monster.disarm_regard_paid` (a controller-review finding — the arc's cheap-regard GUARD: unguarded, set-down/pickup oscillation farms mass becalm, exactly batch 5's dividend risk). **Put-down kind-routing** (amendment A, the §E LIFO-ambiguity WITHDRAWN): `put_down_kind(kind)` + a new byte range `17+len(items)+kind` (set-down-by-kind, mirroring batch 15's use-by-kind `17+kind`); byte 16 stays a back-compat alias for put-down-objective. `SAVE_VERSION` **10→11** (one consolidated bump for the arc; `parse_save` `1..=11`).
  - **T3 — talk-minigame framework + THE POLITE NO + lesson-state** (`1e087a7`). `Minigame` enum `{Echo, AnswerSecondVoice, PoliteDecline}` + `MonsterDef::talk_minigame: Option<Minigame>` (`None` on every existing row). **Load-bearing invariant** (batch-8 empty-pool discipline): `None`/`Echo`/`AnswerSecondVoice` take the UNCHANGED flat receptivity roll — only `PoliteDecline` branches — so every current test/golden/xhash is stable (test-proven `talk_at_a_none_minigame_monster_is_unchanged`). PoliteDecline (mimic): every courteous decline (talk) advances deterministically toward becalm; ending a turn adjacent WITHOUT declining costs `polite_decline_accept_damage` HP; attacking is rudeness. **ECHO verified expressible** (§9-F's own "check") — the rat's echo IS ordinary talk; `Some(Minigame::Echo)` is an inert lesson-tracking tag (sets `echo_done`), no new resolution branch. Lesson-state `echo_done`/`endure_done`/`answer_done` HASHED (the mantel §9-I reads all three); `answer_done` reserved for the coat (cast batch two), set by nothing yet — batch-7-style built-but-unused.
  - **T4 — the mimic's behavior** (`b83fb1c`, committed by controller after the implementer stalled; the one full-run test failure was a build race, confirmed green on the stable tree). Disguise/ambush: a `MonsterDef::starts_disguised` monster (grep-clean flag, not a hardcoded kind) is INERT — no chase/attack — until the player is Chebyshev-adjacent, then reveals PERMANENTLY (one-way) and behaves that same turn; the trigger is the hashed `Monster.disguised` (all construction sites). Glyph stays `c` (disguise is behavioral + a narrative reveal line, not a glyph swap). Courtship register (grounded, workmanlike→wistful) as the PoliteDecline dialogue ladder. Climb re-encounter: a `MonsterDef::climb_reencounter` flag fires a `CarryEvent` when the carrying player passes the becalmed mimic; the McGuffin's reaction is a MARKED PLACEHOLDER (`[YOURS]` deferred, batch-8 empty-pool-safe).
  - **T5 — instrument parity + band re-baseline: the flip HOLDS** (`03eaa89`). Per the instrument-parity rule, the bots learned the batch's new moves IN-batch: the diplomat bots (Pacifist + TacticalPacifist) got `diplomat_decline_byte` (absolute priority to decline an adjacent live non-calm mimic — the correct human play, at real turn/light cost, NOT a hack); the violent bots needed no change (sword walk-over pickup lands ATK immediately via `recompute_atk`, like the old model). All four bands re-baselined to HONEST measured values. **`--sim-flip 5000`: diplomat 43.7% > violent 40.1%, margin +3.6 ≥ 3 — the flip SURVIVES** (INDEPENDENTLY re-measured by the controller). Two nits fixed: `sim_flip_main` now exits nonzero on a missing band file; the Makefile `flip` comment documents the `flip_n`-mismatch as informational.
  - **T6 — docs + whole-branch review + merge** (this entry).
  - **Verification, batch-final**: `cargo test` **254 (default/minifb) / 258 (backend-term)** (up from 208/212 pre-batch). Dump+frame goldens regenerated ONCE in T1 (D3/D5-isolated), byte-identical through T2–T5. `--solve 10000` 0 unwinnable (min 537/p50 885/p90 1044/p99 1215/max 1476, worst seed 82). `--sim 5000`: greedy 0.2%, pacifist 0.1%, tactical **40.1%**, tactical-pacifist **43.7%**, all in-band, stuck=0; **`--sim-flip` margin +3.6**. `xhash` progression this batch: `c8323b3b…`(b16) → `1ec5bf8…`(T2 fix, `disarm_regard_paid`) → `2797824…`(T3 lesson-state) → **`1f22670bf826471a`**(T4 `disguised`; both backends agree). `SAVE_VERSION` **11**. Sizes under real 1.75.0: minifb **674,160 / 221,732** (15.04% of budget), term **522,608 / 199,192** (13.51%).
  - **The landing + a flag for the human**: the mimic is real end to end — disguise, the polite-no, the sword-Hold economy, and the guaranteed placement, all gate-verified with the flip intact. BUT the guaranteed cast rooms made the game EASIER for everyone (they replace the occasional dangerous D3 vaults and shorten the D5 round trip): violence rose from batch-15's 32.3% to **40.1%**, so the flip HELD but NARROWED to +3.6 and violence is less "doomed" than the arc's earlier ~34%. Not a gate failure (the relational flip is the ratified invariant and it clears; the diplomat bot also understates human diplomacy per batch 16's Goodhart note), but a real DIFFICULTY-DRIFT observation for a future tuning decision (the surgical levers if wanted: the cast rooms' own threat, `polite_decline_accept_damage`, `start_light`). Flagged to the human at the close; **the human plays the mimic before cast batch two is briefed** (the road-to-1.0 gate).
  - **Playtest-pending additions** (alongside batches 2–16): the mimic's chest-disguise reveal (does `c`-then-reveal read as a trap?); the polite-no feel (decline-repeatedly-while-your-light-burns — tension or tedium?); the `accept-damage` value (2 HP, untuned); the sword set-down decision (bank regard vs keep your teeth); the new put-down/set-down selector; and whether the narrowed flip / easier game feels right in a human's hands.
  - **Post-merge playtest tuning (2026-07-27, on `feature/happy-valley` after the batch-17 merge):** human playtest found the mimic's accept-damage read as a "nibble," not a mimic EATING you — and being that mild, the whole polite-no minigame was INVISIBLE (no threat, nothing teaching you to decline). Bumped `polite_decline_accept_damage` **2→7** (a real bite: ~3-4 un-declined adjacent turns is lethal on the D3 HP pool with scarce healing) + sharpened the line to fuse comfort and teeth ("makes you comfortable, and takes a bite. -{} HP"). **A welcome second-order effect:** a real predator hurts the VIOLENT bot (routes adjacent and eats it) far more than the DIPLOMAT (declines, skips the accept path), so **the flip WIDENED +3.6→+9.9 and violence dropped 40.1%→33.7%** — partially UNDOING the batch-17 difficulty-drift (violence back near the arc's doomed ~34%). Cartridge-only (a balance constant + a string), no engine/save change; goldens/frames byte-identical, all four sim bands still bracket (win_pct bounds + flip_margin UNCHANGED, comments re-measured), tests 254/258 green. The deeper legibility half (does the player DISCOVER that declining is the escape?) is watched — if a re-play shows danger-felt-but-move-unclear, the next step is a telegraph.
